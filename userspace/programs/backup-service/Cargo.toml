[package]
name = "serviceos-backup-service"
version = "0.1.0"
edition = "2024"

[dependencies]
serviceos-userspace-runtime = { path = "../runtime" }
serviceos-crypto = { path = "../../../shared/crypto" }

[lib]
name = "serviceos_backup_service"
path = "src/lib.rs"
test = true

[[bin]]
name = "serviceos-backup-service"
path = "src/main.rs"
