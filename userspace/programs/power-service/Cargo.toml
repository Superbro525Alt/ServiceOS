[package]
name = "serviceos-power-service"
version = "0.1.0"
edition = "2024"

[dependencies]
serviceos-userspace-runtime = { path = "../runtime" }

[lib]
name = "serviceos_power_service"
path = "src/lib.rs"
test = true

[[bin]]
name = "serviceos-power-service"
path = "src/main.rs"
