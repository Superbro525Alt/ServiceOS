[package]
name = "serviceos-userspace-catalog"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
build = "build.rs"

[lib]
path = "src/lib.rs"

[dependencies]
serviceos-abi = { path = "../../shared/abi" }
