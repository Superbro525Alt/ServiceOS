[package]
name = "serviceos-kernel-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true

[lib]
path = "src/lib.rs"

[features]
ipc-trace = []

[dependencies]
serviceos-abi = { path = "../../shared/abi" }
serviceos-crypto = { path = "../../shared/crypto" }
spin = "0.9"
