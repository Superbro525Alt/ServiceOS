[workspace]
resolver = "3"
members = [
    "runtime",
    "root-manager",
    "storage-service",
    "log-service",
    "config-service",
    "console-service",
    "status-service",
    "shell-service",
    "sysinfo-tool",
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
