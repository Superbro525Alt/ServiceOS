[package]
name = "serviceos-kernel-virt"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true

build = "build.rs"

[[bin]]
name = "serviceos-kernel-virt"
path = "src/main.rs"
test = false
bench = false

[dependencies]
serviceos-abi = { path = "../../../../shared/abi" }
serviceos-bundle = { path = "../../../../shared/bundle" }
serviceos-userspace-catalog = { path = "../../../../userspace/catalog" }
serviceos-kernel-arch-aarch64 = { path = "../../../../arch/aarch64" }
serviceos-kernel-core = { path = "../../../../kernel/core" }
serviceos-platform-virt = { path = ".." }
spin = "0.9"
