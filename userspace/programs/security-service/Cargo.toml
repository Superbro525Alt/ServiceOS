[package]
name = "serviceos-security-service"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "serviceos-security-service"
path = "src/main.rs"

[dependencies]
serviceos-userspace-runtime = { path = "../runtime" }
