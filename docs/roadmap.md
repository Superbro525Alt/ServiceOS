# Roadmap

## 1. Kernel Foundation

| Status | Area | Work Item |
|---|---|---|
| &#x2705; | Kernel foundation | Firmware handoff, memory discovery, paging, and kernel heap bootstrap |
| &#x2705; | Kernel foundation | Reusable kernel heap allocator with free-list reclamation |
| &#x2705; | Kernel foundation | Interrupt, exception, syscall, and timer foundations |
| &#x2705; | Kernel foundation | Kernel object model, capabilities, and IPC |
| &#x2705; | Kernel foundation | Scheduler, task model, and userspace entry |
| &#x2705; | Kernel foundation | Hardening, tests, and architecture cleanup |
| &#x2705; | Scheduling / memory / faults | Complete real CPU register context switching between unrelated kernel threads instead of the current bootstrap-oriented path |
| &#x2705; | Scheduling / memory / faults | Switch from the current boot-derived paging path to fully kernel-owned page tables (a fuller physical direct map beyond the current identity-offset mapping remains open) |
| &#x2705; | Scheduling / memory / faults | Extend the new memory-object info and range-based mapping syscalls into fuller VM control, including unmap, protection changes, virtual-memory queries, and less runtime-managed shared-range allocation |
| &#x2705; | Scheduling / memory / faults | Extend the new generic object inspection, event, and object-wait substrate into broader shared-memory IPC transport, timer/object coverage, and wider userspace adoption beyond the current task/event/channel/input/packet readiness set |
| &#x2705; | Scheduling / memory / faults | Add fast `SYSCALL/SYSRET` on x86_64 alongside the current interrupt-gate syscall path |
| &#x2705; | Scheduling / memory / faults | Kernel memory-pressure classification and an OOM policy on frame-allocation failure landed in kernel-core: a pressure monitor tracks usable-frame headroom (fresh regions plus the reclaimed pool) and kernel-heap headroom (new free-byte accounting over the free-list allocator, excluding bytes the region table could not track), classifies Normal/Tight/Critical against permille threshold constants, detects level transitions in both directions through a global monitor that records them in a bounded ring and notifies registered listeners, and starts at boot with a real baseline reading so transitions are always measured; on allocation failure `allocate_frame_with_oom_policy` reclassifies pressure, selects a victim (the largest-footprint task marked reclaimable, with root-manager/console/shell-name and non-reclaimable candidates protected, ties to the lowest task id), terminates it fault-style with the distinct OOM exit code (`0x4f4f4d00`) while descheduling its threads and returning its charged frames to the frame allocator, then retries the allocation exactly once — protected-set exhaustion panics with an explicit message; the task system registers the candidate-scan/reclaim hooks at init by scanning scheduler-tracked thread owners, and host unit tests cover threshold classification incl. worst-domain-wins, headroom math, the full victim-selection matrix including every protected class, transition/listener/ring behavior, and retry-once plus exhaustion paths. Honest limits: per-task frame charging hooks exist but no spawn/map call site charges yet (so live footprints are zero until adoption), victim service names are not yet plumbed from the manager so name-based protection is exercised at unit level, a userspace bridge now carries pressure transitions across the ABI (the monitor registers an internal listener that mirrors every transition into the kernel event ring as a structured `Pressure` record with from/to level discriminants; log-service drains it into the shared log stream as `KernelPressureChanged` records on the Kernel domain with severity graded by destination level, and status-service classifies them onto a PRESSURE timeline kind while tracking current level plus transition count in the domain counters and the health-rollup snapshot reply additively at words 16/17), power-service health still reports memory-pressure unavailable until it consumes the new feed, and the OOM entry point has no in-tree caller until charging lands — normal-path allocation and boots are byte-identical |
| &#x2705; | Scheduling / memory / faults | Add preemptive time-slice enforcement, CPU-local run queues, and SMP scheduling and interrupt routing (extended: work-stealing load balancing landed across the per-CPU run queues — an idle CPU scans the other queues in a rotating round-robin order and steals up to two threads whose consecutive idle ticks exceed a 3-tick starvation threshold, and a periodic push-balance pass every 64 ticks moves depth above the 2x-average over-commit limit from the busiest to the emptiest queue; per-CPU steal counters, steal-attempt and rebalance-move totals flow through the existing scheduler snapshot/status path (`SchedulerSnapshot.stolen_threads_total`/`rebalance_moves_total`, `steal_stats_line()`), and debug builds print a periodic `sched: steal-stats` serial line whenever more than one CPU participates, while single-core boots keep balancing disabled and scheduling order unchanged; thread affinity hints do not exist yet, so every queued thread is stealable by construction with a single `thread_stealable` gate marking where hints must be consulted once descriptors grow cpusets; host unit tests cover threshold/batch steal selection, round-robin victim rotation, rebalance split math, longest-idle-first candidate ordering, and counter increments; an SMP=2 boot reaches desktop-ready with zero faults and live `steal-stats` evidence lines showing queue depths spread across CPUs) |
| &#x2705; | Scheduling / memory / faults | Add alternate x86 timer sources: LAPIC timer is calibrated, armed, and now the active tick source, and an HPET driver is present and validated behind ACPI discovery for future wake scheduling |
| &#x2705; | Scheduling / memory / faults | Extend the current fault-state propagation, manager supervision, and desktop fault surfacing into richer user-fault upcalls and recovery policy (extended: the x86_64 fault path now classifies user page faults — null-deref vs wild-addr vs exec-of-nonexec vs permission — and packs the class plus low-32 faulting address (or instruction pointer for non-page exceptions) additively into the existing user-fault exit word (tag bits 63..48, address 47..16, class nibble 15..12, legacy vector/error-code detail byte-for-byte unchanged at 11..0), records a per-thread last-fault entry on both the terminate and registered-handler upcall branches, and prints a richer terminate line with class/addr/ip; root-manager supervision grew per-app recovery policies selected through the existing manifest `restart=` grammar — legacy `on-failure:max[:backoff]` unchanged, new `fail-stop` keeps the task failed for the operator without restart, and new `supervisor:&lt;service&gt;[:max[:backoff]]` sends the designated supervisor a KernelTrap fault-report event (faulted service id, packed exit word, supervisor id) over the existing manager&#x2192;log event boundary before applying the same backoff/restart-budget logic, with no new ABI surface — plus crash-loop escalation that overrides any policy to fail-stop once 3 crashes land within 1000 ticks, announced by a loud console `CRASH LOOP` line and an Error-severity event, and manager status replies expose the resolved policy kind and supervisor in two trailing additive words; host unit tests cover the classification function, exit-word packing incl. legacy decode, the full policy decision matrix, crash-window ring math, and the manifest parse matrix. Desktop fault surfacing has since landed in `desktop-shell-service`: a faulted managed app whose exit word carries the user-fault tag is decoded shell-side against the same packing and posted as an actionable crash notification ("App crashed — &lt;app&gt;: &lt;plain-language class explanation&gt;, address 0x…") with null-deref/wild-addr/exec-nonexec/permission explanations, plus Reopen ([R]) and Dismiss ([D], per-entry) actions in the notification overlay where Reopen relaunches through the existing launch-or-focus path and Enter on a crash notice reopens it; host unit tests cover the class&#8594;explanation map and notification payload assembly from packed exit-word decode. Still open: supervisor-directed remediation beyond notify-then-restart, and adoption of the new policy shapes across shipped manifests) |

## 2. Root Bootstrap and Core Service Management

| Status | Area | Work Item |
|---|---|---|
| &#x2705; | Root bootstrap | Root userspace service manager |
| &#x2705; | Root bootstrap | Persisted boot-store manifest index |
| &#x2705; | Root bootstrap | Dependency-ordered startup |
| &#x2705; | Root bootstrap | Capability-scoped startup grants |
| &#x2705; | Root bootstrap | Manager-mediated registration and discovery |
| &#x2705; | Packages / services | Add dynamic service installation, on-demand activation policy, and richer health-check definitions in manifests and package flows |
| &#x2705; | Observability / supervision | Add richer root-manager supervision and health policy without moving service lifecycle policy back into the kernel |
| &#x2705; | Bootstrap / service graph | Explicit boot-mode selection now reaches root-manager end to end on every graph-capable loader: host tooling validates `SERVICEOS_BOOT_MODE=full|reduced|safe|recovery` loudly (invalid values fail before build/image), qemu-virtio/qemu-isa/virt/raspi5 compile the selected startup word 0/1/2/3 into their root-manager bootstrap message while unset stays byte-identical full mode, `cargo xtask recover` remains the recovery convenience path, root-manager's core-set pruning matrix is covered directly for full/reduced/safe/recovery, and the declarative e2e suite now includes a bounded qemu-virtio recovery-selector case proving word 3 plus the pruned storage+console+backup graph without shell/desktop witnesses; riscv64-virt remains out of scope because it has no userspace graph |
| &#x2705; | Bootstrap / orchestration | Extend the current blocked-dependency diagnostics with cycle explanation and startup timing/ordering visibility for service bring-up (manager logs per-service ms/tick start-to-ready durations, an ordering line, total plus slowest-three summary at bring-up completion, and the concrete A&#x2192;B&#x2192;A path when a blocked-dependency chain closes a cycle) |

## 3. Foundational Services

| Status | Area | Work Item |
|---|---|---|
| &#x2705; | Services | `storage-service` |
| &#x2705; | Services | `console-service` |
| &#x2705; | Services | `config-service` |
| &#x2705; | Services | `log-service` |
| &#x2705; | Services | `status-service` |
| &#x2705; | Services | `network-service` |
| &#x2705; | Services | Explicit lookup policy and per-handle transfer-right control |
| &#x2705; | Services / policy | Extend the current service capability-template inspection and scoped on-demand delegation model with explicit revocation flows and richer delegated-capability review |
| &#x2705; | Services / status | Extend the current manager-structured graph and service status into broader per-service health/status surfaces across foundational services |
| &#x2705; | Services / resilience | Add service restart backoff, crash-loop detection, degraded-service state, and manager-facing escalation policy |

## 4. Storage, Filesystem, Packages, and Configuration

| Status | Area | Work Item |
|---|---|---|
| &#x2705; | Storage / filesystem | Replace the current in-memory writable overlay with persistent writable backing |
| &#x2705; | Storage / filesystem | Add writable storage and directory capabilities as first-class scoped authorities instead of broad ambient write access |
| &#x2705; | Storage / filesystem | Extended the mount inventory, composed namespace root, and relative directory-capability traversal with explicit capability-gated mount/unmount mutation (busy-check, atomic table updates, mount table persisted across reboot), multi-backend composition (persistent block-backed plus in-memory temp instances under longest-prefix resolution), and stat/find metadata-query protocols |
| &#x2705; | Storage / policy | Add broader user-home and storage policy, plus writable project/workspace directories and persistent build outputs |
| &#x2705; | Configuration | Add namespaced service configuration trees, write and update policy, and schema validation and migration |
| &#x2B1C; | Storage / indexing | Partial: file indexing and search primitives landed additively inside `storage-service` with no ABI change — an incremental in-memory namespace index (path, kind, size, service-monotonic write tick where tracked, lowercased name tokens) is maintained at every existing mutation entry point (file/directory create, delete, blob write resize; rename/move hook ready for when those ops exist), rebuilt lazily on the first query after boot with a dirty flag and last-rebuild tick exposed as an additive `SIDX` tail on stat replies; two local tag pairs extend the storage contract (search `0x521/0x522`: AND-semantics token queries ranked exact > prefix > substring with subtree scope, size-range and recency-window filters, cursor-paginated one-hit-per-reply like find; grep `0x523/0x524`: bounded text search within a scoped subtree with a 4 KiB per-file cap, 16-result cap, streaming needle matcher carrying line numbers, and oversize-skipped/truncated flag words); host unit tests cover token ranking tiers, event-driven invalidation without TTL expiry, and grep bound enforcement. A storage consistency checker (fsck) landed additively in `storage-service` with no ABI change: an automatic quick-scan runs at mount validating mount-table and entry invariants (path validity, kind/slash rules, duplicates, orphaned entries under no resolving mount) with warn-only findings logged; a local tag pair extends the storage contract (fsck `0x525/0x526`, root-authority gated, dry-run default) driving full scans that additionally walk both persistent snapshot slots — header/layout field validation, generation-chain regression across the slot pair, per-record occupied/kind/path/data-range checks, orphan detection against live mounts, and an FNV-1a64 metadata checksum written at offset 52 by every flush (zero means absent so pre-existing snapshots load untouched) — and a repair flow gated behind explicit operator action that only drops provably-bad records (invalid or duplicate paths, kind-slash mismatches, entries under no mount), releases their handles, forces the derived search-index rebuild, and re-persists the cleaned snapshot; host unit tests cover each corruption class, layout-field rejection including v1 compatibility, FNV-1a64 vectors and incremental hashing, drop-plan idempotence across repeated repairs, survivor preservation, and finding-cap behavior. Client-facing content discovery surfaces (desktop search apps, editor integration) remain open |
| &#x2705; | Storage / sharing | File-management operations landed in `files-app` on the existing storage contracts with no ABI change — keyboard (Delete confirms a destructive delete, F2 rename, Ctrl+N new folder / Ctrl+Shift+N new file) and pointer flows (quick click selects or navigates directories, second click on the same row opens an actions menu — Delete/Rename/Duplicate/Move-to — whose drawn rows are hit-tested so clicks map exactly onto rendered entries); create/delete/rename/move/copy compose from the directory-create/remove and chunked read/write contracts (`(IPC_MAX_WORDS-3)*8`-byte inline chunks), move/rename = copy-then-delete since no rename primitive exists on the wire (recursive tree copy/delete for folders with depth guard and bottom-up removal so the server's non-empty guard never trips), copy/move-on-self is guarded, name collisions auto-suffix "name (2)" preserving extensions against the live listing, prompts accept QWERTY scancode text with shift casing and backspace; failures surface as friendly modal messages mapped from storage statuses (read-only/denied, exists, not-empty/busy, not-found, invalid name, oversize) instead of crashes, long copies render a bounded-cadence percent progress bar mid-operation; host unit tests cover status→message mapping, name validation/path composition/kind suffixing, unique-name variants, copy chunk/percent math, move-plan decision, prompt scancode map, context-menu geometry/hit-testing, and dialog key-flow transitions on a handle-less state; storage-service boot selftest gained a mutation probe logging create=2/rename=1/delete=2 evidence through the direct entry+index layer these ops compose. Still open under this row's original scope: explicit multi-user file-sharing/ACL policy beyond the single-operator scoped-writable-mount model |

## 5. Platform Tooling and Package Foundations

| Status | Area | Work Item |
|---|---|---|
| &#x2705; | Tooling | `shell-service` operator environment |
| &#x2705; | Tooling | `package-service` install, update, rollback, and activation coordination |
| &#x1F7E8; | Packages / signed feeds | Partial: integrity-level feed signing landed in package-service — feeds (repository catalogs and the boot catalog) can carry a trailing `sig-key=`/`sig-digest=` signature computed as a keyed double-FNV-1a digest over the sorted canonical content lines, honestly documented as an integrity checksum bound to a locally pinned secret rather than cryptography; a per-source keystore (`state/packages/feed-keys.cfg`) pins up to four keys per source with one active key and an old-key acceptance window (`window <ticks>`), sync and cached-feed replay verify signatures against the pinned key (mismatch/unsigned-required/stale-window hard-fail with `VerificationFailed`, distinct reject reason word, an Error log event, and a persisted bounded reject journal at `state/packages/feed-journal.cfg`; unknown signer degrades to Unverified requiring explicit operator accept through the existing trust flow), key rotation runs via maintenance action 5 promoting an enrolled key to active then rewriting the keystore config, and boot-trusted sources fail closed (empty catalog + loud event) when keys are pinned but the boot index lacks a valid signature. Host unit tests cover digest stability/order-insensitivity/key-binding, tamper rejection, rotation window gating, missing-key gating, keystore roundtrip, and reject-journal wraparound. Real Ed25519 verification has now landed in package-service alongside the FNV fallback: the pure-Rust `serviceos-crypto` crate implements SHA-512 and Ed25519 (field/scalar/point arithmetic, sign+verify per RFC 8032 §7.1 including the 1024-byte vector) with malleability (S >= L), non-canonical-y, and wrong-algorithm rejection; feeds may carry `sig-alg=ed25519` + `sig-key=` + `sig-sig=` trailers verified against `ekey` entries persisted in the keystore codec, while legacy `sig-digest=` keyed-FNV feeds keep verifying unchanged. Host fixture generation flows through `serviceos-crypto::host`. Key provenance/ceremony and a proper trust root remain open |
| &#x1F7E8; | Packages / updates | Partial: whole-system update transactions landed on the package-service/shell pair — `sysupdate plan` dry-runs the ordered affected-package set (installed slots with a selectable newer catalog target, ascending service-id order), `sysupdate apply [--yes]` executes that set as ONE operation-journal entry (`JOURNAL_SYSUPDATE`) with a persisted transaction file carrying the commit-marker state machine (planning→applying→committing→committed, applying→failed, committed→rolling-back→rolled-back) and a per-step cursor written after every package activation, ending in a durable committed marker plus bounded history-ring append; `sysupdate rollback [--yes]` restores every package of the last committed transaction in reverse order through the existing per-package rollback slots (cursor counts completed reverse steps so a resumed rollback never re-applies updates), and `sysupdate history` renders the recent transaction ring (tick, applied count, rolled-back flag); interrupted applies/rollbacks participate in the existing stale-journal recovery (`pkg recover` resumes from the persisted cursor and completes or discards, reporting resumed/resume-failed honestly; repair also drops the orphaned transaction file). Replies fit the unchanged 16-word IPC budget via a packed id-pair payload; no shared-ABI edits. Host unit tests cover ordering/dedupe/clamping, reverse-plan construction, the full transition matrix and final markers, txn-file codec roundtrip/rejection/caps, history ring trim and codec, journal staleness of the sysupdate action, and the shell-side reply decode/id-unpack grammar. Boot-proven headless on qemu-virtio: desktop-ready + shell-ready with zero faults and live serial runs of plan/apply/rollback/history on a current system (empty-plan paths); a synthetic multi-package apply was NOT drivable in-box because the boot catalog ships no newer-than-installed versions, so multi-step execution rests on the unit-tested model plus reuse of the already-boot-proven per-package activation path. Cross-surface GUI flows, delta/image-based updates beyond package activation, automatic scheduling, and signed update manifests remain open |
| &#x2705; | Packages / key management | Operator feed-signing-key management landed in shell-service + package-service — a new additive `PackageTag` range (0x720..0x729, KeysList/Enroll/Activate/Rotate/Gen with two new `PackageStatus` values 14 invalid-parameter and 15 already-exists) drives the existing ed25519 keystore directly: `pkg keys list` walks the flat source+key table rendering one row per pinned key (src/id/alg/state/active/retired-tick), `pkg keys enroll <source> <hex-pubkey>` validates the 64-hex ed25519 pubkey service-side and stores it under a material-derived auto id (`k-<fnv16>`, mirrored by a host-tested shell-side preview) so the first key of a fresh source bootstraps active while later ones enroll as retired standbys, `pkg keys activate <id>` resolves the owning source from the live listing then promotes that key through the existing rotate-active path (retiring the previous active at the monotonic now), `pkg keys rotate <source>` promotes the MOST recently enrolled retired standby for whole-source rotation and prints which id became active, and `pkg keys gen <source> [--show-seed]` generates a fresh ed25519 seed guest-side via `shared/crypto` SHA-512 mixing of the source name, monotonic tick, per-boot counter, and a keystore FNV fingerprint (pure-`core`, so signing material IS available in-guest; only the public half is persisted and the seed is returned once in the reply, never stored) with an honestly documented entropy caveat — the kernel has no RNG yet so guest seeds are uniqueness-grade substitutes, not CSPRNG output, and production keys should still be host-generated then enrolled. All mutations persist the keystore before replying so a crash never advertises un-persisted state, error codes map onto explicit wire statuses, and every helper is covered by host unit tests on both sides of the IPC boundary (id derivation/hex encoding/generated-identity variance+enrollment validity/rotation semantics/error mapping/wire field packing plus reply unpacking roundtrips). Full in-guest FEED signing with generated seeds (a sign control op over the stored-public flow) remains open |
| &#x2705; | Packages / trust | Add package trust state, source provenance, signing/trust status, and rollback provenance inspection through the package-service contract |
| &#x2B1C; | Packages / policy | Extend the current package pinning and channel/ring policy with real staged rollout cohorts and richer per-source upgrade rules |
| &#x2705; | Packages / recovery | Add better interrupted-install recovery, consistency checking, garbage collection, and repair flows for partially applied updates |

## 6. Architecture and Platform Abstraction

| Status | Area | Work Item |
|---|---|---|
| &#x2705; | Architecture | Explicit `kernel/core`, `arch/<isa>`, and `platform/<platform>` layering |
| &#x2705; | Architecture | `arch/x86_64` as the active ISA crate |
| &#x2705; | Architecture | `platform/x86_64/qemu_virtio` for UEFI, serial, framebuffer, input, and VirtIO backend wiring |
| &#x2705; | Architecture | `arch/aarch64` and `platform/aarch64/raspi5` as first-class targets with a native Raspberry Pi 5 image and serial-first userspace bootstrap |
| &#x2705; | Architecture | Platform-first `xtask` build, image, and run selection |
| &#x2705; | Architecture | Normalized `BootInfo` handoff into generic kernel initialization |
| &#x1F7E8; | Platform / hardware follow-on | Partial: Raspberry Pi 5 framebuffer path landed behind the current `platform/aarch64/raspi5` contracts — new `mailbox.rs` implements the Broadcom VideoCore property-channel driver per spec (16-byte-aligned static envelopes with full-slot zero padding, request/response codes, GET_FIRMWARE_REVISION probe, ALLOCATE_BUFFER align=16, SET_PHYSICAL/VIRTUAL_WIDTH_HEIGHT, SET_DEPTH, SET_VIRTUAL_OFFSET, GET_PITCH; per-tag completion/error-bit validation plus the response≤request rule; bus-address translation helpers `physical_to_bus`/`bus_to_physical` carrying a documented assumption that BCM2712 firmware keeps the legacy 0x4000_0000/0xC000_0000 DRAM alias windows; DTB-discovered MMIO base via compatible `brcm,bcm2835-mbox` with parent-`ranges` translation instead of pinned board constants; polled FULL/EMPTY handshakes with quarter-second counter-derived timeouts and foreign-channel draining). Framebuffer stub replaced by a real two-phase flow: pre-MMU `negotiate()` runs three serialized transactions (revision probe → geometry+depth+offset+allocation envelope → pitch query), converts validated payloads through `assemble_geometry` into kernel-core `FramebufferInfo`, feeds BootInfo.framebuffer plus the image's MMIO region list (mailbox regs and scanout window both mapped), and post-MMU `initialize_backend()` exposes the same DisplayBackend contract shape as x86 qemu_virtio (whole-frame present + row-granular damage present) registered into the kernel display-object registry; any failure degrades gracefully to serial-first/headless exactly like the old stub. Protocol is host-unit-tested through `tests/protocol.rs`, which includes the production module by path (golden ALLOCATE_BUFFER wire layout, builder/validation/geometry rejection matrix, roundtrip build→in-place-parse, translation roundtrip; lib target keeps `test=false` because kernel-core's kernel-image global allocator would poison dependent host harnesses). HONEST LIMIT: UNTESTED WITHOUT HARDWARE — no BCM2712 run has ever exercised mailbox exchange or scanout presents; QEMU cannot emulate the raspi5 VideoCore/firmware pair (`xtask run --platform raspi5` stays ManualDeploy), so hardware-facing behavior rests on spec conformance plus the unit-tested pure layer until a real Pi 5 boots it. Remaining open in this row: graphical input, networking, writable boot-store, and audio backends |
| &#x2B1C; | Platform / hardware follow-on | Add Wi-Fi and Bluetooth controller backends behind the relevant `platform/<platform>` contracts as supported hardware targets expand |
| &#x2B1C; | Platform / hardware follow-on | Expand Raspberry Pi 5 beyond the current serial-first bootstrap into the normal graphical and network-backed service graph |
| &#x2705; | Platform / targets | Add `platform/aarch64/virt` or equivalent clean ARM virtual target to keep ARM bring-up separate from Pi-specific board quirks |
| &#x2B1C; | Platform / targets | Add a second x86 platform target: `qemu-isa` (legacy BIOS/SeaBIOS PVH boot, serial-first) boots the full kernel and userspace bootstrap; final userspace entry transition still faults (see docs/handoff-qemu-isa.md). Splitting x86 PC interrupt-controller details out of `arch/x86_64` remains open |
| &#x2705; | Platform / targets | `riscv64/virt` skeleton landed: new `arch/riscv64` crate (SBI legacy-console print, all-traps stvec handler that hang-loops with cause/sepc/stval, one-shot timer via the SBI TIME extension plus `rdtime`, QEMU-virt memory-layout constants) and a `platform/riscv64/virt` + image pair linking at 0x80200000 (OpenSBI `-bios default` payload entry; DRAM base 0x80000000 is firmware-owned), building for `riscv64imac-unknown-none-elf` with xtask build/image/run plumbing (`cargo xtask run --platform riscv64-virt` boots under qemu-system-riscv64, prints the serial banner, and parks hart 0); deliberately out of scope and still open: MMU/paging (runs bare-metal on the identity map with `satp` untouched), trap dispatch beyond the hang handler, interrupt-driven ticks (timer interrupts stay masked), DTB parsing, device drivers, and any userspace/catalog plumbing |
| &#x2B1C; | Platform / targets | Add additional future-facing targets such as real x86 PC hardware once the current platform layering stabilizes |

## 7. Networking

| Status | Area | Work Item |
|---|---|---|
| &#x2705; | Networking | Grow the current networking surface beyond DHCP/DNS/ICMP/outbound TCP into UDP, inbound/listening transports, richer routing, multi-interface policy, and IPv6 |
| &#x2B1C; | Networking | Add long-lived resolver caching, richer DNS record support, firewalling, and broader network policy (partial: resolver caching landed — network-service now runs its own DNS-over-UDP client and wire codec in place of the smoltcp DNS socket, with a TTL-honoring positive/negative cache for A/AAAA/CNAME records (negative TTLs capped so operator-side fixes propagate), bounded CNAME chasing (8 hops) that follows both fully-appended in-packet chains and cross-query links, AAAA and TXT record parsing, distinct NXDOMAIN/SERVFAIL/NODATA/timeout detail codes appended to ResolveReply and a new typed ResolveEx contract, and resolver hit/miss counters carried in the trailing InterfaceStatusReply word; firewalling landed — an ordered first-match allow/deny rule table over protocol+direction+port with per-rule hit counters, inbound/outbound deny totals, and a settable default-inbound policy, enforced at outbound TCP connect / UDP send-to / ICMP ping and at inbound TCP accept / UDP receive, settable and queryable over the existing public network control channel via reserved tag values 0x80e–0x813 pending promotion into shared/abi; host unit tests cover cache TTL expiry, chain-depth bounds, and the firewall rule-match matrix; still open: IPv6 datagram plumbing beyond record parsing, richer policy sources (per-interface rules, address sets), promotion of the reserved tags into the shared ABI, and desktop-facing firewall/resolver state surfaces) |
| &#x2B1C; | Networking / wireless | Add Wi-Fi device support, network scanning, join/auth flows, saved networks, and wireless configuration behind the current network-service contract |
| &#x2B1C; | Networking / wireless | Add richer wireless policy including roaming, security modes, per-network trust/configuration, and desktop-facing wireless state surfaces |
| &#x2705; | Networking / performance | Partial: packet-buffer sharing landed on BOTH directions — RX: a `PacketInterfaceRingSetup` syscall (52) negotiates a memory-object-backed shared RX ring between the kernel packet-interface object and network-service (one header page + one page per slot so frames never straddle physical pages; kernel-owned head / service-owned tail free-running sequence counters with drop-oldest overflow matching the legacy queue), the kernel fills the next slot directly from the backend receive (a single copy out of the driver queue) and returns a doorbell instead of copying into caller IPC buffers, and the service consumes frames IN PLACE for all its hot paths via a borrowed-slot RX token; TX (new): a mirrored `PacketInterfaceTxRingSetup` syscall (53) plus a `PacketInterfaceTxRingFlush` doorbell (54) give the service the producer side of an identically laid-out image — the smoltcp TX token writes each outbound frame straight into the next mapped slot (one slot write replacing the per-frame IPC/syscall copy), rings the doorbell, and the kernel drains published slots through the backend transmit path with slot lifecycle kept IN USE until completion (tail advances only after `backend.transmit` succeeds, banking tx-copies-avoided/bytes-saved credit per frame); documented copy strategy: the kernel COPIES slot payload into the driver-owned TxBuffer because the virtio-drivers API (`VirtIONet::send`) only accepts buffers it allocated itself, so direct user-page-to-descriptor mapping is not possible and the eliminated copy is the IPC boundary one, mirroring RX; fallbacks preserved end to end — negotiation failure, the `tx-ring=off` hosts line, a full backlog, oversize frames, or a stall watchdog (8 consecutive undrained doorbells) all revert to the legacy copied-transmit path with a greppable revert line; zero-copy statistics live in each ring header and surface through `rx-ring stats` / `tx-ring stats` log lines after selftest plus reserved public-channel tags `0x822`–`0x823` (additive TX words 5..=7); host unit tests cover TX FIFO wraparound with free-running counters past slot_count, slot-in-use-until-transmit-completes including Busy-retry with credit banked exactly once, and the no-ring copied fallback. Honest limits: the device-to-ring copy remains on RX and the slot-to-descriptor copy remains on TX (both documented), and live qemu-virtio boots negotiate both rings (`tx-ring enabled slots=16`) with selftest passing green, but under the current tree the kernel-side drain observed an empty head/tail view of the freshly created TX image (service-side publishes were visible only service-side), so the stall watchdog reverts outbound traffic to the copied path after the threshold while DHCP/address-config and net-selftest stay green — root-causing the cross-view visibility gap for late-created memory objects is the open follow-up before tx-copies-avoided climbs live |
| &#x2B1C; | Networking / drivers | Add richer NIC interrupt models such as MSI/MSI-X, additional virtual backends, and real NIC driver hosts beyond current QEMU/VirtIO |
| &#x2B1C; | Networking / services | Partial: host naming, local service discovery, and diagnostics tooling landed additively inside `network-service` with no ABI change (reserved public-channel tags continuing the firewall/resolve-ex convention) &#x2014; host naming: runtime hostname get/set tags `0x814`&#x2013;`0x817` plus an optional `hostname=` / `mdns=` / `discovery=` line in the hosts resource file (default name `serviceos`; runtime set is session-scoped), with a default-ON mDNS-LITE responder on UDP 5353 that answers unicast A/ANY queries for `<hostname>.local` using the shared DNS codec, a compression-pointer answer owner name, and cache-flush class &#x2014; honestly a subset: no multicast group operation, no probing/conflict resolution, no SRV/TXT/PTR or DNS-SD; discovery: a service-local UDP beacon on port 41453 (magic/version/flags wire format) broadcasting periodic announce + query solicitations from a small registry populated via control tags (`0x81e`&#x2013;`0x81f`), and a peers query (`0x820`&#x2013;`0x821`) returning hosts announced within a caller-supplied window (LRU eviction at 4 entries, same-name refresh); diagnostics: continuous-ping op (`0x818`&#x2013;`0x819`) wrapping the existing ICMP path for N sequential probes with per-packet RTTs folded into min/max/avg/jitter/permil-loss stats, an ARP-snooped neighbor dump (`0x81a`&#x2013;`0x81b`) observed off the RX path, and a port-scan-self helper (`0x81c`&#x2013;`0x81d`) listing TCP listeners, client UDP sockets, and internal service ports (mDNS 5353, beacon 41453); host unit tests cover local-name matching + compressed answer encoding, beacon encode/decode bounds, RTT stat math, and registry/peer-table refresh-expiry-eviction. Full mDNS/DNS-SD/LLMNR semantics, real multicast operation, cross-host interop validation, and desktop-facing discovery/diagnostics UIs remain open |

## 8. Graphics and Session Foundation

| Status | Area | Work Item |
|---|---|---|
| &#x2705; | Graphics / session | Kernel display-output object |
| &#x2705; | Graphics / session | `graphics-service` output, surface, and composition ownership |
| &#x2705; | Graphics / session | `session-service` focus and graphical session ownership |
| &#x2705; | Graphics / session | Shell/operator inspection of outputs, surfaces, and sessions |
| &#x2705; | Graphics | Grow the current damage-tracked multi-buffer graphics path into multiple outputs and sessions, explicit release/fence-style presentation sync, and broader compositor-side partial-present support. Fence-style presentation sync v0 landed: every compositor present issues a monotonic frame-counter fence (FenceTracker issue/complete high-water math), `PresentBufferReply` now carries a trailing fence token computed conservatively over the remaining same-turn damage burst (`fence_for_request`), clients can query completion through the extended output-status reply (trailing word 12 = completed-fence counter alongside words 13–15 holding no-op skip count, bytes saved by skips, and close-pending surface count), and destructive surface closes arriving while a frame is still pending are deferred (`close_pending`) and flushed only after the covering frame presents, so a surface cannot vanish mid-present; host unit tests cover fence lifecycle/ordering math and conservative burst budgeting. Compositor-side partial-present landed on top of the existing per-rect damage path: the service keeps a presented-frame shadow, compares each damage region before issuing the kernel present-damage call, and turns byte-identical regions into skipped presents whose region sizes accumulate into the status-reply savings counters; full-frame presents get the same identical-frame skip. Display mode management contract landed in the kernel display object: `DisplayModeInfo` mirrors the BootInfo-derived geometry (w/h/bpp/stride), the backend trait enumerates supported modes with an honest single-entry default for boot framebuffers, and `set_mode` validates against that list — the active mode is an accepted no-op, listed-but-unswitchable modes return Busy, unlisted geometry returns Unsupported — so the syscall-level contract is complete ahead of any real mode-set; kernel host tests cover mode mirroring, exact-match lookup, and the set_mode decision matrix. Multiple outputs v0 landed inside the compositor service: graphics-service now owns an output registry (primary = boot framebuffer, plus one on-demand memory-backed virtual mirror output created via a service-local `0x910` control op that reports `CapacityExceeded` when the registry is full and `Denied` for zero-sized geometry), every present records per-output present/noop-skip/saved-byte counters, `OutputListReply` gained trailing output ids and `OutputStatusRequest` accepts an enumeration index with per-output stats (word 1 stays the index so existing clients are unaffected), and after each primary present the mirror refreshes through a nearest-neighbour blit whose damage rect is scaled from primary coordinates — identical regions become per-output noop skips; host unit tests cover registry ops/capacity, per-output stat isolation, blit math (identity, stride mismatch, nearest-neighbour up/downscale), damage-rect mapping/clamping, and shadow-tracked skip behavior. Honest limits: capacity is one virtual output today, the kernel display object still exposes exactly one real output (no second virtio-gpu scanout), and nothing in-tree sends the create/extend control ops yet — exercise them via raw `0x910`/`0x914` messages or wait for a runtime wrapper. Client-side fence waiting landed as a service-local control op (`0x912` request carrying token + timeout ticks, `0x913` reply carrying status + completed counter, same additive policy as the `0x910` output-create pair): already-complete tokens answer immediately through the completed-high-water rule, zero timeouts degrade to an honest bounded poll, positive timeouts park the reply handle in a fixed-capacity waiter list that the main loop reaps after every compositor present (completion side) and once per turn (timeout side, inclusive deadline math with saturating `now + timeout`), replies use `Ok`/`Busy`/`CapacityExceeded`, and the loop's receive timeout shrinks to the earliest pending deadline so clients block on the reply instead of spinning; host unit tests cover completion/expiry classification, saturation, inclusive expiry boundaries, high-water reaping of multiple waiters, timeout-only reaping with still-pending retention, and full-list handle hand-back. Damage-tracked partial flush landed on top of the presented-frame shadow: every damage present and full-frame present first diffs its clip region against the shadow row-by-row (stride padding excluded), merges changed rows into disjoint scanline bands (overlapping/adjacent damage-rect rows unite), and when the changed area is a strict subset (<50% of frame bytes) flushes only those bands through per-band kernel present-damage calls while mirroring exactly those rows into the shadow — identical regions still skip entirely, ≥50% or over-capacity diffs fall back to the whole-clip present, and band savings accumulate separately from noop-skip savings in per-output counters plus bounded `partial-flush savings:` evidence logs; host unit tests cover band merging from damage rects, capacity overflow, skip/partial/fallback planning at the threshold boundary, stride-padding immunity, and savings accounting. EXTEND mode v0 landed on top of the mirror registry: virtual outputs are no longer forced to mirror — a new service-local control op pair (`0x914` request carrying output id + side word, `0x915` reply carrying status, desktop-space render origin, and the combined bounds rect; same additive policy as `0x910`/`0x912`) places a virtual output LEFT-of or RIGHT-of the primary with its own geometry (top-aligned, zero gap), the primary stays anchored at desktop (0,0) and cannot be re-placed, per-output render-origin math exposes where each output sits in desktop space, combined desktop bounds are computed as the union of the primary rect with every EXTEND-placed output rect (bounding box when heights differ — documented), and pointer-clamp math keeps cursor positions inside those combined bounds so the pointer cannot fall off the desktop edge when traversing outputs; EXTEND-placed outputs stop being refreshed from the primary's presented frame (mirror-mode refresh is unchanged) while their independent content pipeline remains future work. Host unit tests cover right-of/left-of placement math incl. negative-x left-of, primary/unknown-id rejection, side-word table validation, revert-to-mirror clearing placement, bounds union for both sides with differing heights, pointer clamping at corners/interior/far-out plus the no-primary None case, and render-origin lookups. Still open: real multi-head scanout beyond the single boot framebuffer (the kernel display object still exposes one output), independent content rendering for EXTEND-placed virtual outputs (placement/bounds/pointer math are live but nothing paints into their rectangles yet), typed client-side wait wrappers in the userspace runtime on top of the raw `0x912` op, runtime/shell wrappers for the raw `0x914` pair, real display mode-set behind the new contract, and per-surface (rather than per-turn) fence accounting |
| &#x2705; | Graphics | Grow the current shared-buffer graphics path into mapped or zero-copy presentation buffers and a broader client-render protocol for richer clients |
| &#x2B1C; | Graphics | Add richer display mode management and eventual GPU-accelerated composition without collapsing graphics policy into the desktop shell |
| &#x2705; | Input | Finish the move from receive-side polling to fully device-driven input wakeups; the platform IRQ path is active, but the current session/input stack still needs a one-shot nonblocking receive fallback to avoid missed-edge stalls |
| &#x2B1C; | Input | Partial: multi-host input support landed additively — the VirtIO input backend now enumerates every physical instance distinctly instead of aggregate counts (`InputDeviceInfo` carries a stable `source_id`, device class keyboard/pointer/tablet from the role-unification pass, role flags positional-authority/scroll-only, and hot-plug presence), surfaced via `InputBackend::enumerate_devices` / `InputSourceObject` passthroughs and logged per-instance at platform bring-up; every event now tags its originating host through a new trailing `InputEventInfo.source_id` field that flows unchanged through the receive syscall, userspace runtime, and session/desktop consumers (secondary hosts are never dropped: a relative pointer demoted by the single-positional-stream policy still routes its buttons and wheel under its own tag, only its motion is suppressed); hot-plug tolerance marks removed instances absent so polling skips them entirely (a stale device cannot wedge the shared pipeline), their queued events are ignored, and presence is restorable; host unit tests cover instance enumeration decode, source-id tagging across receive paths, and stale-source absence handling. Still open: true multi-seat hardware-to-session assignment, runtime (re-)enumeration over a control channel rather than bring-up-only visibility, and per-source routing/priority policy surfaces in session-service and the desktop shell |
| &#x2B1C; | Session / composition | Partial: multi-session registry landed in `session-service` (sessions carry ids and seat bindings), with a switch-active control operation that performs staged input-route detach, outgoing-focus teardown, and seat transfer; isolation policy enforces per-session app-surface membership on focus and kernel-input routing so apps of inactive sessions receive no pointer/key events, and session listing plus current-session queries ride the existing contracts (host unit tests cover the handoff state machine and isolation matrix); per-session desktop/compositor instances, operator login/session ownership, and true multi-seat hardware routing remain open |

## 9. Desktop Shell and Interaction

| Status | Area | Work Item |
|---|---|---|
| &#x2705; | Desktop shell | `desktop-shell-service` graphical shell chrome and launcher surface ownership |
| &#x2705; | Desktop shell | Manager-mediated graphical app launch and focus handling |
| &#x2705; | Desktop shell | First core app set: `settings-app`, `files-app`, `monitor-app` |
| &#x2705; | Desktop shell | Serial-shell inspection of desktop state through the desktop-shell contract |
| &#x2705; | Desktop interaction | Window focus, z-order, move, resize, minimize, restore, and close handling in `desktop-shell-service` |
| &#x2705; | Desktop interaction | Maximize handling in `desktop-shell-service` |
| &#x2705; | Desktop interaction | App-control channel for desktop-driven focus, resize, close, pointer, key, and text events |
| &#x2705; | Desktop interaction | Physical input routing from the kernel input-source object through `session-service` into the desktop interaction contract |
| &#x2705; | Desktop interaction | Shell/operator inspection and control of desktop windows through the desktop-shell contract |
| &#x2B1C; | Desktop shell | Finish the remaining shell follow-on work with gesture/snap/tiling policy, smoother window transitions, fuller open-with and default-association flows, and richer system-status surfaces on top of the current notification history and workspace model |
| &#x2B1C; | Desktop shell | Partial: hold-and-cycle MRU task switching landed — Alt+Tab now opens a preview overlay (per-app tiles with icon-style initial plus name, highlighted selection, live selection reordering) where Tab/Shift+Tab cycle the MRU ring with wrap-around and releasing Alt commits only then (Esc cancels without refocusing), and the command palette routes broader global actions (toggle media/volume, toggle/clear overlays, dismiss-all notifications, settings page cycling for a running Settings app via the existing key contract, lock-session shell stub) ranked alongside app launches by the shared launcher scorer; host unit tests cover MRU promote/wrap ordering, candidate filtering, open-selection skip-focus math, and apps-vs-actions ranking merge. Smoother animation timing and fuller gesture policy remain open |
| &#x2B1C; | Desktop shell | Extend the current app-focused command palette, launcher ranking, app switching history, and workspaces into document/content search and multi-monitor shell behavior. Partial: the palette half landed — with a two-or-more-byte query the shell queries the existing storage-service name index over its storage lookup grant (search `0x521/0x522`, one-hit-per-reply cursor pagination) and ranks up to four file hits strictly below the app/action block with guaranteed result slots and kind icons (`[]` file, `[/]` directory), selecting a hit commits through files-app's OpenPath intent; a leading `/` switches to grep-backed content mode (grep `0x523/0x524`) showing path plus line number; degraded conditions (no storage handle, transport errors, malformed/end replies) collapse to the plain app/action list rather than stale hits. Still open for this row: launcher-panel ranking of documents outside the palette, multi-monitor shell behavior |
| &#x2B1C; | Desktop shell | Partial: inter-app file handoff landed on the existing channels without ABI change — files-app press-and-move drag gestures arm a shell-side content-drag state (payload rides the desktop notify channel behind a reserved control prefix: undecided-target digit for drags, explicit app digit for open-with), the launcher panel becomes a live drop target with hover highlighting, and pointer-up routes the drop decision (launcher icon &#x2192; launch/focus plus app-control OpenPath intent to that app, bare canvas &#x2192; reveal in files-app, back over a window &#x2192; cancel; 900-tick expiry and source-exit cleanup guard stale drags); files-app gains extension-based open-with cycling (O) and default-association commit/clear (D) over a pure association table (upsert/remove/candidates/codec), a global recent-files ring (R view, move-to-front dedup, capacity 8, codec) recorded on every successful open, and persistence of both tables under `state/files-app/` via the existing writable storage directory pattern with graceful in-memory degradation; generalized deliver-open-intent hands OpenPath to any desktop app though only files-app consumes it today. Host unit tests cover intent encode/parse bounds and hint mapping, drop-decision routing, drag expiry, table ops/routing/ext extraction, ring dedup/capacity/codec, and gesture threshold state transitions. Still open: richer drag ghost visuals beyond launcher highlight, per-app open-intent consumers beyond files-app, multi-file drags, and non-global (per-extension) recent rings |
| &#x2B1C; | Desktop shell | Extend the current launch-denied and package/runtime security surfaces into broader permissions-aware desktop UX for app authority, file/device access, notification policy, and runtime launches |
| &#x2B1C; | Desktop shell / wireless | Add desktop Wi-Fi UX for scanning, joining, saved networks, signal state, and connection troubleshooting |
| &#x2B1C; | Desktop shell / Bluetooth | Add desktop Bluetooth UX for pairing, device management, battery/state display, and audio/input routing |
| &#x2B1C; | Apps / UI | Add broader graphical application and toolkit/runtime layers on top of the current app and window foundations |

## 10. Terminal, Shell, Console, and Operator UX

| Status | Area | Work Item |
|---|---|---|
| &#x2B1C; | Shell / sessions | Partial: multiple shell/operator sessions landed in `shell-service` — the shell hosts an operator-session registry keyed by source (the serial console session plus client sessions opened over the shell public channel, each with a dedicated endpoint), every row owns its own command-history ring and optional account identity, and the main loop is now an event loop that drains client input while the serial read-line request stays armed; `sessions` / `history [count]` give operator views of the registry and per-session history, and host unit tests cover session keying/capacity/reuse, ring wrap + consecutive-duplicate collapse, and ownership scoping. Login/session ownership policy landed additively against account-service's published contract tags — `login <name> <secret>` launches the stored account-service image on demand through the manager's stored-image path (no ABI change), binds the returned identity/capabilities word to the active session row, `whoami`/`logout` inspect or unbind it, and when the image is absent or launch denied the session simply stays unowned with a readable explanation since activation is manual. Job control landed additively: `command &` queues a background job (monotonic never-reused ids, bounded eight-slot table that lazily evicts only fully-reported done rows) whose execution the shell event loop drains one-per-tick through the ordinary command path with output captured into the job row instead of a terminal — `jobs` lists id/state/pending-output bytes per row, `fg <id>` streams retained output into the requesting session and reports the exit status (error names preserved), completed jobs announce `[id]+ done …` with exit ok/failed once at the session's next submitted line, and host unit tests cover spawn/state/drain/notify transitions, truncation-once reporting, capacity eviction policy, and monotonic ids. Real kernel pipes replaced the mediated pipeline: `cmdA | cmdB` now crosses an actual `Pipe` kernel object at every stage boundary — a 64 KiB bounded ring with reader/writer handle pairs (`PipeCreate` returns both handles; reads require READ, writes WRITE, both WAIT-able), blocking layered on the object-wait substrate (`pipe_read` parks the caller until data or EOF, `pipe_write` parks when the ring fills with partial writes reported honestly, `PIPE_FLAG_NONBLOCK` degrades both to immediate queue-empty errors), EOF when the last writer handle closes (close/duplicate hooks keep per-side refcounts so duplicated writers delay EOF correctly) and `BrokenPipe` once the last reader is gone. Stage commands remain shell built-ins (the spawn contract transfers exactly one bootstrap capability and stages are compiled into the shell binary, so per-stage spawn-with-handle exec does not apply); the shell pushes each producing stage's captured bytes through the pipe writer handle, closes it to flip the reader side to EOF, and drains the reader into the consumer's input lines — announcing a loud mediated fallback only if the kernel ever declines the pipe syscalls. Opt-in line-consuming stages (`filter <text>`, `count`, argument-less `cat`; non-consuming stages ignore piped input), a four-stage cap, no quoting/redirection syntax, and no streaming commands inside pipelines are unchanged; host tests cover ring-wraparound ordering across the physical boundary, EOF-after-last-writer-close with pending data still readable first, broken-pipe after last reader close, full-ring would-block backpressure recovery, exact-fill partial writes, and per-side refcount snapshots. Package-installed command discovery, richer process environments, and richer status views remain open |
| &#x2B1C; | Terminal | Split panes and explicit session profiles have landed on the current richer ANSI/VT base (per-pane sessions, focus/resize/close keybindings, profile-driven shell metadata); remote terminal sessions also landed as groundwork — but PLAINTEXT and pre-SSH (see the Terminal row below) — so remaining expansion is real SSH/TLS-backed remote sessions on top of the shared clipboard path, themes, and resize/reflow semantics |
| &#x2B1C; | Terminal | Partial: session persistence/reattach, command bookmarking, and session restore landed on the terminal-service/terminal-app pair — the service retains a per-session scrollback ring plus input-line/cursor state and a spare channel duplicate so a pane can detach (Ctrl+Shift+Q closes the pane or tab while the session stays alive), any later attach request mints a fresh client handle and replays the retained grid stream with a redrawn input line, Ctrl+B snapshots the current command line into an eight-entry per-session bookmark list that Ctrl+Shift+B cycles back into the editable line (never auto-executed), and a newly launched terminal enumerates live sessions first to reattach the most recent detached one (with a `[reattached session N]` notice) before opening a fresh shell; host unit tests cover ring retention/eviction, the attach/detach state machine, and bookmark add/dedupe/cycle/evict ops. Remote terminal sessions ALSO landed on this base, honestly as a plaintext rsh-like stopgap, not SSH: terminal-service binds a configurable-port listener (4023) through the network service's existing TCP listen/accept path, each accepted connection passes an optional single-token auth gate (config constant; empty = open, documented insecure) and then becomes a normal session with I/O bridged bidirectionally over length-prefixed frames (`u16be len | payload`, max 512), disconnect detaches rather than kills (scrollback/history survive for a local reattach), and a bounded boot-time loopback self-connect probe is built in but default-OFF because inbound-to-self across services is not yet drivable end-to-end (the network stack proves loopback with its internal raw-socket selftest, but an IPC-driven connect to our own listener currently stalls before adoption — boot diagnostics verified; unit-level host tests cover framing/auth-gate/link-state instead). The port must be allow-listed by the network firewall for non-loopback peers (default inbound-allow admits it today). No encryption or integrity protection exists — SSH/TLS-backed sessions remain open, alongside richer command/result inspection; the durable-profile-persistence gap has since closed — the terminal app now stores its profile set (theme picks included) as state/terminal/profiles.cfg on the persistent storage mount and restores it on launch, and a Ctrl-R incremental reverse search over each pane's submitted commands runs as a local overlay (shell-service hosts the pure search state machine with host tests) |
| &#x2B1C; | Console | Partial: graphical console surface landed in `console-service` — every formatted kernel/service record is mirrored into a retained 80x24 VT text grid (ring ordered, newest rows win) and pushed to subscribed console sessions over the existing write-shaped contract, with alternate-screen-style opt-in markers carried inside `SessionWriteText` payloads so no ABI edit was needed; operator-session handoff/routing landed alongside — a client loss now detaches the row (history ring retained) instead of destroying it and the next session open adopts the detached row, mirroring terminal-service detach/reattach semantics. The shell's `console grid` / `console follow` render snapshots and live tails of that stream wherever the executing identity holds a console-service grant, and host unit tests cover grid encoding (reset prefix + padded CRLF rows), ring wrap, clamp/padding, and marker parsing. Viewing the stream inside a terminal-app pane still requires a terminal-to-console lookup grant plus pane-side subscription UX (manifest/app work outside this slice), so per-client graphical panes remain open |
| &#x2705; | Operator UX | Landed richer shell/operator diagnostics on the service contracts: `logs follow` streams a live filtered tail through log-service subscriptions and ends cleanly on Ctrl-C over console sessions (graphical terminal panes fall back to an idle timeout), `logs crashes` lists recent crash-shaped records by filtering the retained log ring client-side until log-service dispatches a dedicated crash-query tag, `status health` renders the status-service snapshot rollup as a formatted table, and `status svc <name>` / `ps app [name]` give structured per-service and per-app inspection |

## 11. Execution, Loading, Compatibility, and Runtimes

| Status | Area | Work Item |
|---|---|---|
| &#x2705; | Runtime foundation | Package-delivered `runtime-service` for explicit compatibility/runtime environments |
| &#x2705; | Runtime foundation | Environment creation, inspection, run launch, and teardown through a real userspace service contract |
| &#x2705; | Runtime foundation | Explicit mount and variable mapping for the first `posix` runtime profile |
| &#x2705; | Runtime foundation | Manager-mediated launch of runtime-hosted workloads instead of shell-owned compatibility shortcuts |
| &#x2705; | Runtime foundation | Shared shell/terminal operator integration for runtime inspection and launch |
| &#x2B1C; | Loading / execution | Grow the current flat-image-plus-ELF native loader into dependency loading, richer executable/runtime policy, and broader native image format support (partial groundwork: the kernel loader's static-ELF64 path — direct PT_LOAD segment parsing with the same user-window containment and per-segment W^X/NX policy as flat images — is now reachable end-to-end from `runtime-service`, whose exec launch route stages stored image bytes into a memory object and classifies them with the loader's own fallback order, flat v2 → flat v1 → raw ELF64, before manager-mediated `LaunchImage`/`TaskSpawnImage`; PIE groundwork landed: the loader now accepts `ET_DYN` images, choosing a deterministic page-aligned base at the bottom of the user image window and mapping PT_LOADs exactly as `ET_EXEC` minus fixed vaddrs (entry and image base become base-relative), then applies `.rela.dyn` `R_X86_64_RELATIVE` fixups discovered through `PT_DYNAMIC` (`DT_RELA`/`DT_RELASZ`) by writing `base+addend` at `base+offset` through the mapped frames with per-target bounds checks — any other relocation type fails with a distinct `UnsupportedRelocation`; window containment, per-segment W^X flags, and GNU_STACK NX policy are unchanged, and host unit tests cover a golden minimal ET_DYN (three RELATIVE relocs applied into a byte buffer), bad-type rejection, malformed dynamic blocks, out-of-bounds targets, and deterministic base selection. Dynamic linking landed (S11): the loader now parses `.dynsym`/`.dynstr` and the SysV `DT_HASH` table out of `PT_DYNAMIC` (`DT_HASH` is required — it supplies `nchain`, bounding dynsym reads; GNU-hash-only images fail symbol relocation with `UnsupportedRelocation`), registers every mapped module's defined global/weak exports into a fixed-capacity per-load symbol namespace (a later strong registration overrides an earlier weak one and later registrations override earlier equal-strength ones, so the main image — registered last — can override its dependencies), and applies `R_X86_64_GLOB_DAT`/`R_X86_64_JUMP_SLOT` alongside `R_X86_64_RELATIVE` through one bounds-checked applier that runs only after all images are mapped: flat-image dependencies may now be ELF64 `ET_DYN` shared objects mapped at the same deterministic companion bases (ELF class/machine/type validated, exports registered during mapping via the hash buckets, each companion's relocations applied once the namespace is complete); unresolved weak symbols resolve to 0 while unresolved strong symbols fail the load with a new `UnresolvedSymbol { name }` error surfaced as `Unsupported` by the spawn syscall mapping; host unit tests cover SysV hash bucket/chain lookup (single-bucket collision chains), export filtering of locals/undefs plus load-base biasing, override ordering including main-last-wins, cross-module GLOB_DAT + JMPREL JUMP_SLOT resolution with weak-undefined=0 and a mixed RELATIVE entry, strong-missing failure carrying the symbol name, consumer-dynsym requirement for symbol reloc types, and golden two-module fixture bytes exercising the whole parse → register → resolve → apply path. Real split main+library binaries await packaging support, so kernel-side coverage is via the unit harness. Interpreter policy and lazy-PLT compatibility landed (S11 follow-up): the loader now parses `PT_INTERP` and enforces a distinct policy — any interpreter path other than the built-in marker `serviceos-ld` fails the load with a new `InterpreterUnsupported` error (surfaced as `Unsupported` by the spawn syscall mapping; companions naming an external linker keep that distinct error rather than flattening into `DependencyInvalid`), while the marker opts into exactly the existing built-in link semantics since no external `ld.so` exists. Lazy PLT binding landed as an eager-with-latch hybrid rather than true call-time binding: a real lazy resolver needs to execute at first call (a userspace runtime linker or a kernel fault/syscall hook with per-task linker state persisted past load), none of which exists, so JUMP_SLOT slots are still bound eagerly at load — but every mutation is recorded in a PLT latch (`slot, resolved value, dynsym index`) and the canonical x86-64 lazy geometry (three reserved GOT.PLT slots, 16-byte stub stride, PLT0 at offset 0) is parsed (`DT_PLTGOT`/`DT_PLTREL`, RELA-form enforced) and pinned by tests, so binaries shaped for lazy semantics run unmodified off the eagerly-patched GOT and host tests replay the resolver flow against the latch to prove both paths converge on identical GOT state; true deferred resolution (trampoline page, RW→RX transition, call-time patch) remains open along with relro and richer runtime packaging. Windows-runtime groundwork landed honestly at the DETECTION layer in `runtime-service`: a new PE/COFF parser (`pe.rs` — DOS header + `e_lfanew`, `PE\0\0` signature, COFF machine/section-count/timestamp/optional-header fields, PE32+ optional-header magic/entry-RVA/image-base/subsystem, section-table extraction capped at 16 entries with the declared count reported) classifies x86_64 PE32+ as the roadmap target shape and rejects wrong-machine, PE32, and unknown-magic images with reasons; the guest-image exec classifier now answers a distinct `WindowsPe` verdict for any parseable MZ image so launches refuse with `Unsupported` instead of a generic unknown-format error. Detection/classification only — no execution: the real mountain is a WinAPI/WinNT ABI layer (PEB/TEB layout, syscall thunks, loader semantics) that does not exist yet; env profiles accept `kind=windows` today so Windows-targeting environments can be declared ahead of the runtime, documented as accepted-but-unexecutable until that layer lands. Host unit tests cover a hand-built golden minimal PE32+ fixture (classification plus COFF/optional-header field extraction and `.text`/`.data` section round-trip), truncated/bad-signature rejection, i386/aarch64 machine rejection, PE32-magic rejection on x86_64, section-table cap plus over-claimed-count corruption, non-ASCII section-name honesty, corrupt-MZ staying UnknownFormat, and windows-kind profile acceptance |
| &#x2705; | Loading / execution | Add a general process loader for user-supplied images instead of only manager-owned stored images |
| &#x2B1C; | Compatibility | Grow the current compatibility/runtime foundation beyond hosted `posix` environments into Linux-oriented ABI expansion, arbitrary ELF execution, richer runtime packaging, and desktop launch UX for runtime-hosted apps (partial groundwork: `runtime-service` gained a guest-exec spawn path for arbitrary *static* ELF64 images resolved through environment mounts and gated by the same sandbox matrix as hosted workloads; a host-testable Linux-x86_64 syscall-number translation skeleton maps the console/handle/memory/clock/exit families onto existing ServiceOS syscall numbers with everything else resolving ENOSYS by contract — no in-guest dispatch consumes it yet, so no kernel changes were needed; environment descriptors may declare bundled guest libraries via `lib = name = path` lines that are carried on the environment record and validated as mappable flat images at launch, though kernel dependency mapping still flows through the stored-image resolver rather than arbitrary per-launch bytes; still open: dynamic linking/PIE, live syscall dispatch through the translation layer, fd-table semantics, desktop launch UX, and shell surfacing of guest-exec runs) |
| &#x2B1C; | Compatibility / security | Add explicit capability grants for network, graphics, input, and audio to compatibility workloads (partial: runtime-service environments now carry a per-environment device-class grant matrix over network/graphics/input/audio that is default-deny — requested classes derive from the environment profile's `caps` line, grants and revocations flow exclusively through the existing pending-approval → env-decision flow (`Allowed` with mask / `Blocked` / `DefaultAllow`) and land in the security audit trail as `RuntimeApprovalRequested`/`RuntimeApprovalChanged`; run launch consults the matrix-derived decision and refuses workloads on environments with requested-but-ungranted classes or a denied approval through the unchanged `PendingApproval`/`Denied` status contract, and `EnvStatusReply` gained trailing requested/granted class-mask words for inspection. The input class is reserved groundwork — shared ABI `runtime_capability` has no INPUT bit yet, so it can never be requested and stays deny-only until the wire contract grows one; per-workload declarations and syscall-level per-class enforcement inside hosted guests remain open) |
| &#x2B1C; | Compatibility | Add Windows runtime support and broader cross-platform application execution |
| &#x2B1C; | Compatibility / security | Add stronger sandboxing and container-style isolation for compatibility workloads (partial groundwork: each runtime-service environment record now persists a sandbox profile — the per-device-class grant matrix above — alongside its capabilities, mounts, and variables for the lifetime of the environment, and launches gate on a single matrix-derived decision function mirroring `RuntimeEnvState` semantics, host-tested for defaults, transitions, and sync; container-style isolation, per-workload sandbox manifests comparable to the developer-service worker `PermissionSet`, namespace/cgroup-style boundaries, and cross-reboot environment persistence do not exist yet) |
| &#x2B1C; | Runtime UX | Add desktop-facing runtime launch surfaces, runtime state inspection, and app/runtime distinction UX for native versus hosted applications |

## 12. Audio and Media

| Status | Area | Work Item |
|---|---|---|
| &#x2B1C; | Audio | Grow the current audio surface beyond tone playback: PCM output streams with format negotiation (U8/S16/S32/F32, mono/stereo, rate table), chunked IPC writes (blocking/nonblocking), N-stream mixing with clipping protection, and per-stream + master volume/mute landed on a mixed-PCM sink validated by boot selftest and host unit tests. Audible output landed behind the `SERVICEOS_AUDIO=1` xtask flag: a virtio-sound PCI backend driver (`platform/x86_64/qemu_virtio` sound module) probes QEMU's `-device virtio-sound-pci`, configures its first output stream for S16LE stereo at 48 kHz, and receives every mixed batch through a new `AudioEndpointPcmWrite` kernel syscall; the boot selftest replays both synthetic streams into the device and prints accepted queue bytes as evidence, and the endpoint reports `VirtioSound` vs PC-speaker/null honestly. Without the flag (or when no device is present) the mixer falls back to the null sink exactly as before. Capture landed as an honest null-capture pipeline on the reserved contract: `CaptureOpen` (0x898/0x899) now negotiates format/rate/channels inline (same table as playback) and returns a per-stream control handle, new additive `CaptureRead*` tags (0x89a/0x89b) serve nonblocking and blocking reads whose silence frames are real-time paced against the 100 Hz monotonic clock (a reader can never pull more frames than elapsed ticks justify), carry first-frame timestamps, and fold into the same s16-stereo FNV-1a checksum unit as playback; closes report produced-frame totals and stream status/list replies gained capture direction and counters, while playback-only tags on capture slots answer mirrored `Unsupported`. The virtio-drivers 0.13 crate exposes input-stream enumeration but its PCM path is TX-only (`pcm_xfer`/`pcm_xfer_nb` queue on the transmit stream alone), so the platform driver probes device input streams into the bringup summary for honesty (a QEMU mic exists only when the host `-audiodev` supplies input, e.g. `QEMU_AUDIODEV=driver=pa,id=speaker,in.rate=48000`) and capture synthesizes digital silence until the crate grows an RX transfer; the boot selftest prints a paced capture sweep evidence line (`selftest capture ok reads=N frames=M sum=… tick=…`) and host unit tests cover pacing math, per-format silence packing, checksum folding, and selftest determinism. True device RX capture, per-app policy, and notification/media controls remain open |
| &#x2B1C; | Audio / hardware | Add broader hardware audio backends beyond the current wired/emulated paths, including Bluetooth audio and future USB audio paths |
| &#x2B1C; | Audio / Bluetooth | Add Bluetooth audio output/input support, pairing integration, and endpoint routing on top of the broader audio platform |
| &#x2B1C; | Media | Add codecs, containers, richer media pipelines, DMA-safe memory-object policy, and broader hardware backends beyond the current QEMU PC speaker path |
| &#x2B1C; | Audio / UX | Partial: desktop-facing volume and endpoint status surfaced — desktop-shell gained a media overlay (Alt+M or command palette) that honestly reports output endpoint presence/state (including no-endpoint and audio-unavailable states), master volume/mute with keyboard control wired to the audio-service set-master-volume contract (`EndpointVolumeSet`), and active/listed PCM stream counts; the settings System page shows live PCM stream counts beside endpoint state. Master volume is set-only in the ABI (no read-back tag), so the shell displays its last applied value, and per-stream formats are visible only at the sink level; true endpoint selection, per-app audio policy, and notification/media playback integration remain open |
| &#x2B1C; | Media / apps | Partial: a first-party media app (`media-app`) now provides graphical playback/preview on top of the existing PCM contracts — it scans its storage scope for audio-capable files (`wav`/`wave`/`pcm`, bounded two-level walk with sorted deduplicated list), parses RIFF/WAVE headers (uncompressed PCM tags 1 bits 8/16/32 and float-32 tag 3) and routes them through a new pluggable codec pipeline: header-magic container sniffing picks a fmt-tag decoder from a static registry, so PCM variants keep decoding as before while a real block-based IMA-ADPCM decoder (tag 0x11 - per-channel predictor/step-index headers, 4-byte-per-channel interleaved sub-blocks decoded low-nibble-first, Microsoft truncation rule for partial tails, initial predictor emitted as first frame) converts byte chunks to normalized f32 frames fed into an F32Le wire stream; unknown encoding tags and malformed compressed headers fail loudly with distinct UNSUPPORTED FORMAT / CODEC UNSUPPORTED notices instead of playing noise, streams file bytes through audio-service StreamConfigure/StreamWrite chunked IPC writes sized to fit both the message-word budget and the service-side decode buffer (nonblocking writes with busy backoff across loop ticks so the UI stays live), supports per-stream SetVolume volume/mute from the keyboard and buttons, drains and closes the stream on completion, and posts a plain notification-history event ("MEDIA finished/stopped <track>") through the existing desktop notify contract; the shell-side desktop-shell media overlay listing active PCM streams remains. Files-app open-with now routes audio extensions to the media app by default (explicit picks and stored overrides still win) via the shell content-intent handoff that delivers the path to the running app, and `desktop open media` / launcher / palette entries can launch it standalone. Host unit tests cover WAV header parse (supported/unsupported encodings, data-length clamping, per-tag constraints incl. block-align sanity and wSamplesPerBlock capture), codec reference vectors hand-computed against the IMA step/index tables, stereo channel independence, chunked-vs-single-shot decode equality, PCM passthrough normalization parity, sniff dispatch, and error paths, per-format sample decode normalization, IPC/service-buffer chunking math, extension routing, and open-with default precedence. IMA-ADPCM WAV has landed as the first compressed win above raw PCM, but mp3, ogg/vorbis, seeking/gapless pipelining beyond strictly sequential streaming, DMA-safe memory-object policy, and broader hardware backends beyond the current QEMU paths remain open |

## 13. Developer Tooling and Workflows

| Status | Area | Work Item |
|---|---|---|
| &#x2705; | Developer tooling | Package-delivered `developer-service` for toolchain, workspace, build-job, and artifact management |
| &#x2705; | Developer tooling | Packaged toolchain descriptors for native, Linux, Windows, and honest remote-only macOS target metadata |
| &#x2705; | Developer tooling | Packaged workspace descriptors and sample source payloads for the first cross-target workflow |
| &#x2705; | Developer tooling | Manager-mediated launch of transient `cross-builder-tool` workers instead of shell-owned build shortcuts |
| &#x2705; | Developer tooling | Shared shell/terminal operator integration for toolchain inspection, build submission, job inspection, and artifact export |
| &#x2B1C; | Developer workflows | Grow the current developer tooling foundation beyond packaged sample workspaces into broader SDK/toolchain distribution, richer language ecosystems, and runtime-aware build/run workflows (partial: build-worker sandboxing groundwork landed — developer-service now derives an explicit per-job capability manifest from the workspace descriptor plus toolchain SDK root with network denied, records the allow decision on the job record, hands the permission set to `cross-builder-tool` at launch, and the worker echoes `worker sandbox:` and cleanly fails out-of-scope jobs with a distinct sandbox-denied report status; in-service toolchain registry landed — developer-service derives family (rust/gcc/llvm/native), dotted version, newest-first per-family rank, and a live storage presence probe for each packaged descriptor, serves them as trailing fields on the existing toolchain list/info replies, and refuses builds whose installed toolchain SDK root no longer resolves; runtime-aware routing landed at the decision layer — `BuildRequest` accepts an optional runtime profile tag, the service resolves it against active runtime-service environments over the runtime env-list contract and records the route on the job record with a `worker route:` echo, falling back to the existing direct worker spawn whenever no tag is given or no matching ready environment answers. SDK payload distribution landed — toolchain descriptors accept `payload=<name>@image:<boot-store-id>` or `@path:<storage>` references with an optional per-payload `checksum=` (sha256, hex, case-normalized), the install operation runs at catalog load and materializes path payloads into the writable mirror `state/devsvc/sdk/<toolchain>/<name>` verifying declared checksums over the copied bytes while image-id payloads materialize as `.imgref` reference stubs because boot-store bytes stay kernel-authoritative and unreadable from userspace, verify-present is payload-aware (descriptors declaring payloads are confirmed by their materialized files; bare roots keep the old probe), and installed/failed counts ride the catalog-loaded log word. Executing builds inside routed environments landed at the service boundary — env snapshots now carry granted capability bits from the env-list contract, routed jobs intersect the job permission set with the environment's grants under minimum-privilege rules (fs scopes survive only with FILE_READ granted and network stays denied even when the env has it), selection runs through a host-tested matrix, and accepted routes launch through runtime-service's guest-exec `RunLaunchRequest` contract with a duplicated log handle so the builder payload stages and writes artifacts inside the environment's mounts; every refusal — missing FILE_READ grant, unusable intersection, exec-status refusal, or transport failure — falls back to the direct spawn with the full original permission set, and explicit execution-mode words (direct / routed-env / routed-fallback plus reason) record on the job, serve as new trailing IDE-tail fields on job info and the 0xd20 snapshot, and feed the worker route word so the echo states how the job actually ran. Host unit tests cover payload-ref parsing, sha256 vectors and checksum gating, mirror layout planning, descriptor payload lines, intersection rules, the full mode-selection matrix, and mode/status-word encodings. Still open: exit/completion propagation from runtime-service runs back to developer job records (routed jobs remain Running with the run owned by runtime-service), packaging an actually executable SDK image so routed exec completes end-to-end in-box (today's packaged content exercises the fallback leg by design), and checksummed remote fetch for richer language ecosystems) |
| &#x2B1C; | Developer workflows / infra | Add remote build farms and remote macOS build/sign/notarization integration on top of the current honest remote-only target model (partial: remote farm registry landed — developer-service derives one endpoint record per remote-only toolchain descriptor keyed by its target, recording an optional descriptor-provided connection string (`remote_endpoint=`), and build requests against remote-only targets resolve through that registry instead of failing obscurely: unconfigured endpoints answer an explicit not-configured refusal, configured endpoints without a live network service answer explicit unreachable status with the recorded endpoint echoed back, and registered targets queue a job whose artifact references stay marked exported-pending against the endpoint until a farm fetches them; export marking surfaces through additive IDE tail words on job/artifact replies, and host unit tests cover registry derivation, target matching, dispatch decisions, and reply packing. Packaged endpoint strings, the actual network dispatch transport, and build/sign/notarization hand-off remain open) |
| &#x1F7E8; | Developer workflows / infra | Partial next step: a loopback LIVE accept-path harness landed in developer-service — compile-time gated behind `SERVICEOS_FARM_SELFTEST=1` at userspace build time (`option_env!`, so default boots are unchanged), it queues one synthetic remote-target job through the standard slot/route model (RemoteFarm route + PendingRemote export against a `self@127.0.0.1:<port>` endpoint), arms a real TCP listener through the network service's SocketListen contract (the runtime crate exposes outbound sockets but no listen wrapper yet, so developer-service speaks that leg in raw ABI messages), connects back to itself over guest-internal loopback, sends a minimal `FARMQ1 <job-id> <target>` dispatch frame, runs the in-process accept half of the wire protocol (magic+id validation, exact-id ACK framing), flips the queued job Queued→Running→Succeeded on acknowledgement, and emits greppable boot evidence (`farm-selftest PASS job=... target=linux-x64 endpoint=127.0.0.1:<port>` plus an end ok=N summary line; FAIL logs name the exact stalled stage within a hard bounded spin budget so boots can never hang). A local control tag pair (0xd22 request / 0xd23 reply) lets tooling trigger the same harness on demand without rebuilding. Wire framing, id validation, ack matching, and the queue-state transitions are unit-tested on host; the activation path additionally required declaring `depends=network-service` / `grant=network-service:send` on the developer-service bundle manifest (previously even the registry probe could never see the transport); payload upload, remote build execution, and farm authentication remain open |
| &#x2B1C; | Developer UX | Add IDE/editor integration and desktop-facing developer workflow UX without bypassing the shared shell/runtime path (partial: machine-readable editor hooks landed additively on the existing developer-service control contract — job list/info and workspace info replies gained self-describing trailing tails (`IDE1` magic word embedding the field count) carrying phase (state / route kind / export state), resolved toolchain index, artifact presence/name-length flags, and farm/export status with endpoint length; a dedicated local query tag pair (0xd20 request, 0xd21 reply) returns a single-message job snapshot — status, phase, workspace id plus toolchain index, artifact size/format/handle flag, farm status plus endpoint reference, packed artifact name and endpoint bytes — so external editors can poll one message for everything; the contract shape is carried in the reply words themselves and pinned by host unit tests. Desktop-facing developer workflow UX beyond these reply contracts remains open) |
| &#x2B1C; | Developer UX | Add project browser, artifact viewer, debugger/profiler hooks, and richer build/test/run surfaces in the desktop environment |
| &#x2B1C; | Developer trust / policy | Add explicit developer-tool permissions, workspace trust policy, and project/runtime authority review surfaces |

## 14. Permissions, Trust, and Security UX

| Status | Area | Work Item |
|---|---|---|
| &#x2705; | Permissions | Add clearer app permissions and capability review surfaces for native apps, runtime-hosted apps, and developer tools |
| &#x2B1C; | Runtime permissions | Service-side review flows landed on the existing contracts: pending-request queue query (runtime `EnvList` filtered to `PendingApproval`), approve/deny plus approve-subset grants through the existing runtime decision contract with per-decision approval audit records carrying the granted mask, kind-filtered audit inspection queries on both the runtime and security services, and host unit tests for subset validation, decision decoding, and audit roundtrip; this also fixed a decision-word decode mismatch that landed desktop approvals as denials. Shell launch denials caused by pending runtime approvals now render a structured explanation that points the operator at the approval queue query (`runtime envs`) and the `security runtime <id> approve` decision surface. Packaged runtime profiles requesting sensitive capabilities by default landed additively on the env-profile grammar: descriptors may declare a sensitive-only `requests = network,graphics,audio` line whose words join the environment's capability mask at creation so the sandbox matrix seeds its requested column from declared caps plus explicit requests (non-sensitive or unknown request words are rejected), and the shipped `posix` runtime profile now requests `network` by default — sensible for hosted runtimes — so every new posix environment enters `PendingApproval`, emits the `runtime-approval-pending` audit/log record, and only launches after an operator approves exactly the requested subset through the unchanged decision contract. Actual desktop prompt UX beyond the current settings buttons and persisted grant history remain open |
| &#x2B1C; | Trust / signing UX | Partial: shell operator trust inspection landed — `pkg info <name>` now renders provenance (trust state, derived signing state, channel, ring, update source) plus rollback provenance (availability, previous version, source) from the existing package-service provenance/history contracts, with host unit tests for the signing-state mapping. Extending this into runtime/developer-artifact trust surfacing plus stronger cryptographic signing and trust-root enforcement remains open |
| &#x2705; | Package trust UI | Add package trust UI showing provenance, trust/signing state, update source, and rollback provenance in both terminal and desktop flows |
| &#x2B1C; | Desktop security surfaces | Extend the current settings-based security review page into broader privileged-action, trust-warning, and authority-escalation flows across software installation, runtime launch, and document/open flows |
| &#x2B1C; | Security UX | Partial: shell/operator denial explanations landed — launch denials for native apps (`run sysinfo`), stored images (`run image`), and runtime-hosted workloads (`runtime launch`) now render a structured explanation (reason class, missing authority, review surface, concrete next action) instead of bare denial text, driven by the existing security policy, manager/security/runtime audit, and pending-approval queue contracts, with host unit tests for reason-class mapping and explanation rendering. Broader desktop-facing GUI explanation surfaces across apps and runtime-hosted workloads remain open |
| &#x2705; | Security policy | Add stronger permission editing/review flows, revocation UX, and capability grant history without moving policy into the shell or UI code |
| &#x2B1C; | Security foundations | Build on the current security audit and runtime approval foundation with stronger signing/key management, trust roots, and broader sandbox policy expansion |

## 15. Software Distribution and App Ecosystem

| Status | Area | Work Item |
|---|---|---|
| &#x2705; | Software center | Add a software center / app-store style GUI on top of the package/update foundation |
| &#x2B1C; | Installer / updater UX | Partial: shell/operator installer-updater UX landed on the package-service pair — `pkg install/update <name> [version] [@source]` accepts an explicit source with a trust preview (mode, pinned digest, sync state) and a `--yes` confirmation gate for non-boot-trusted sources; install/update/rollback replies carry per-phase progress counters (resolve/materialize/verify/activate/persist step counts plus percent) that the shell renders; rollback prints a previous→restored version summary with trigger; maintenance replies expose operation-journal status (stale-since-boot detection with startup Warn telemetry) and a `pkg recover` action resumes or discards interrupted installs/updates/rollbacks; host unit tests cover progress phase math, journal staleness classification, and the source-selection grammar. Mutation handlers no longer propagate request errors into service exit. Live streaming progress over the log stream, software-center/desktop surfaces for these flows, and richer trust explanations remain open |
| &#x2B1C; | App lifecycle | Partial: installed packages are wired into launch and review surfaces on the existing contracts — `run pkg <name>` launches an installed package through the manager-mediated stored-image path after a package-service identity check (installed + version echo, already-running instances report phase instead of double-launching, image-less packages say so), and the software-center detail panel carries a launch action that routes to that same shell path (the app holds no manager capability, so launch stays manager/shell-mediated); update/remove visibility landed on both surfaces — `pkg list` rows carry an `update=` flag decided by a numeric dotted-version compare against a catalog snapshot, `pkg info` adds `update-available` plus last-change (newest install/update event tick from the log ring), the shell remove flow prints a pre/post cleanup summary (removed version, deactivation, journal clear, retained rollback slot, explicit `pkg gc` storage-reclamation pointer), and software-center shows the same badge, in-session update ticks, and removal summary; permissions-review handoff landed in the shell denial explanations — launch denials while runtime approvals are pending classify as `RuntimePendingApproval` and point at the `runtime envs` queue query and `security runtime <id> approve` decision surface instead of a bare denial. Host unit tests cover version-compare/decision mapping (shell and app sides) and handoff classification precedence. File/open-with association policy remains open, as do direct in-app launch without the shell hop and package-service-carried update timestamps |
| &#x2705; | App ecosystem | Add richer catalog search with tiered name/description/keyword ranking, a category filter view, and per-package host/target compatibility surfacing backed by software-center catalog metadata. Catalog richness landed: the side-table now carries per-package screenshot references and editorial placeholder ratings, the detail panel renders a screenshot reference as a stylized, honestly-labeled placeholder card (the text framebuffer stack cannot decode images, so real screenshots are not faked), shows a clamped five-star rating bar when a rating is present, and computes a deterministic offline "recommended for you" row from the installed set (same-category install popularity plus keyword overlap with installed apps, zero-score entries dropped, ties keeping catalog order); host unit tests cover recommendation scoring/capping/tie-breaks and installed-base gating, placeholder selection stability, and rating clamp/star-bar mapping. Real image decode for screenshots and usage-telemetry-backed ratings/recommendations remain open |
| &#x2B1C; | App distribution | Partial: third-party repository onboarding, trust review, side-loading policy, and compatibility warnings landed on the shell-driven registration path — `pkg repo add` is now a two-phase onboarding flow (without `--yes` it prints a trust review: endpoint, chosen trust mode with its meaning, what changes once added — installability plus per-trust acknowledgement/sync-fail-closed behavior — and management pointers; only `--yes` commits to package-service and records the source in a shell-side onboarding ledger), onboarded sources are manageable via `pkg repo enable/disable/remove/status` (disabled sources hard-block installs/updates at the shell command level; remove revokes the standing approval and falls back to per-use `--yes` review; status joins the ledger against service state), `pkg repos` rows carry an onboarded/onboarded-disabled suffix; a side-loading policy switch (`pkg sideload policy [allow|warn|deny]`, default warn) gates the local-file image launch path (`run image`) as the side-load trust class with clear per-policy messaging; install/update compat checks parse a declared `+<arch>` version tag (plus `any`/`all`) against the host arch and block mismatches unless `--force-compat` is passed. Host unit tests cover the trust-level gating matrix, side-load decision matrix, compat tag/mismatch detection, and ledger roundtrips. Shell-resident state resets at reboot (package-service has no repository enable/remove protocol yet), the software-center GUI does not surface these flows yet, and undeclared-target packages pass unflagged |
| &#x2B1C; | App policy | Add per-app default associations, intent/open-with policy, recent apps/documents, and uninstall cleanup behavior |

## 16. Desktop Polish and Advanced Interaction

| Status | Area | Work Item |
|---|---|---|
| &#x2B1C; | Desktop polish | Partial: hold-and-cycle MRU task switcher landed on the existing workspace overlays — preview tiles with per-app initial/name, wrap-around cycling, modifier-release commit, and Esc cancel (see Desktop shell row for details); gesture handling, smoother animation timing, and a fuller macOS-like desktop feel remain open |
| &#x2B1C; | Desktop polish | Partial: notification-history quick actions row landed — the history overlay renders a persistent `[A] DISMISS ALL / [F] FOCUS SOURCE` strip (keyboard A/F plus pointer hit zones on the strip), row clicks select and focus the source app, and dismiss-all clears the history ring in place with host unit tests for the quick-action key scancodes and selection clamps; permissions-aware desktop surfaces remain open |
| &#x2B1C; | Desktop polish | Partial: shell-side window transitions landed — open, close, minimize, and restore now run short eased geometry animations (150-250 ms, fixed-point cubic ease-out/ease-in curves stepped from the existing shell frame loop via async surface-geometry updates so request/input handling never blocks), with an animation queue that replaces duplicate per-window transitions, cancels cleanly on user move/resize/app exit, and layered soft drop-shadow bands rendered around the focused window as compositor rect strips that track focus and resize; host unit tests cover easing endpoints/monotonicity, interpolation and clamping, queue replace/capacity/cancel, minimize-target bounds, and shadow strip geometry. True alpha fades/scales, workspace-switch transitions, dock/genie effects, and broader interaction polish remain open pending compositor sampling support (window buffers blit 1:1 today, so "scale" is an eased reveal of animated geometry rather than resampled content) |
| &#x2B1C; | Desktop polish | Partial: shell actions are unified into a single global registry — every action id (app launches plus system actions) is declared once with its palette label/match keywords, exact-modifier keyboard binding, hot-corner assignment, and quick-action key, and that one table feeds the command-palette ranking, hot-corner dispatch, the notification quick-action strip, and the global shortcut path alike (Alt+1..5 launch apps, Alt+N/M overlays, Ctrl+Space palette, Ctrl+Alt+V clipboard history, Ctrl+Alt+1..4 workspace switching, Ctrl+Alt+Shift+1..4 move the focused window between workspaces, Ctrl+Alt+H/J/=/- accessibility toggles); registering a new action is one enum variant plus one registry row; host unit tests cover corner/binding/quick-key lookups, exact-modifier disambiguation (Alt+1 vs Ctrl+Alt+1), binding and label uniqueness, and catalog reachability; permission-aware per-app action surfaces remain open |
| &#x2B1C; | Desktop polish | Partial: the clipboard history panel is now interactive — pointer clicks (and Enter) activate a history row through the existing clipboard-service `Activate` contract, promoting it to the current clipboard and forwarding Ctrl+V down/up to the focused app so terminals paste immediately, with unavailable-service and out-of-range rows degrading to notices instead of errors; window drag gestures landed on the shell — dragging a titlebar into the 16 px left/right screen-edge bands shows a rubber-band outline of the target half (compositor rect strips on a dedicated gesture overlay surface) and releasing tiles the window to exactly that half below the topbar, dropping into the bottom-edge band minimizes instead, and Ctrl+Alt+Left/Right trigger the same half-snap through the action registry; content drag-and-drop beyond files-app-to-shell handoff and richer file handoff remain open |
| &#x2B1C; | Desktop polish | Partial: accessibility settings and desktop gestures landed on the shell — a persisted access config (`state/desktop-shell/access.cfg`, storage-service directory pattern with graceful in-memory degrade) stores three settings toggled from the keyboard: high-contrast mode (Ctrl+Alt+H) swaps every shell-rendered surface to a black-panel/white-text theme with high-signal status colors via a shell-side theme layer (topbar, launcher, status panel, task switcher, command palette including its CPU-composited buffer, notification/clipboard/media overlays; app-window interiors stay app-rendered), reduce-motion (Ctrl+Alt+J) short-circuits the shared animation queue at enqueue time so open/close/minimize/restore transitions apply instantly while easing math is preserved, and a magnifier hook (Ctrl+Alt+= / Ctrl+Alt+- stepping 1x/1.5x/2x) applies an integer focal-point geometry transform around the cursor over visible app windows plus the launcher/status panels with inverse pointer mapping keeping hit-testing consistent, auto-disengaging whenever an overlay, drag, resize, or queued animation would fight the transformed geometry (compositor blits buffers scaled-to-geometry, so content enlarges geometrically rather than being resampled). Four hot corners with small dwell (24 px exclusive edge bands, 12-tick hold, re-arm on leave) fire shell actions: top-left opens the Alt+Tab task switcher, top-right the notification history, bottom-left the command palette (launcher), bottom-right a show-desktop toggle that minimizes all visible windows and restores exactly that set on re-trigger; host unit tests cover corner zone mapping incl. offscreen rejection, dwell arm/fire-once/leave-reset/corner-switch restart, zoom step clamping, focal transform identity/fixed-point/bounds clamping, pointer unmap inverse, reduce-motion gate, theme variant swap, and access-config codec roundtrip. True pixel resampling for the magnifier, per-app assistive hooks such as sticky keys or an on-screen keyboard, and gesture vocabulary beyond the new Ctrl+Alt+Up mission-control workspace overview (a registry-bound overlay grid of all workspaces with per-workspace window counts and active marker, navigable by arrows/Enter/click to switch) remain open |

## 17. Observability, Logging, and Status

| Status | Area | Work Item |
|---|---|---|
| &#x2705; | Logging / observability | Add persistent log storage, streaming log subscriptions, richer structured payload schemas, and better kernel trap ingestion into the log pipeline |
| &#x2705; | Status / observability | Add richer service health reporting, subscription-based status monitoring, and shell/session status views |
| &#x2705; | Observability / desktop | Add crash-record capture and query in the log service and a system-health rollup in the status snapshot reply (total services, counts by state, degraded/restarting lists, worst restart offenders) on top of the current logging/status foundations; desktop-facing log/crash viewer surfaces and app/runtime diagnostics remain open |
| &#x2705; | Observability / developer | Add an operator-level event timeline to the status service: a bounded ring of notable per-service events (registration seeds, phase state transitions, restart/backoff crashes, health flips) stamped with monotonic ticks, windowed last-N and since-tick query replies, a summary view with per-service counts, first/last event ticks, and the busiest-service stat, plus seeded/live timeline stats log lines; validated by host unit tests for ring retention, ordering, and summary math and by boot selftest evidence. Domain diagnostics v0 landed for builds and graphics: the status service subscribes to the existing log stream (Developer and Graphics domain feeds) and classifies developer build-job phase transitions (queued→running→succeeded/failed/unsupported) plus compositor present-count pacing anomalies into dedicated timeline event kinds (job-phase 5, frame-pacing 6); the summary reply gained per-domain counters in words 12–15 (jobs started/succeeded/failed by final state, long frames), with host unit tests for classification and counter aggregation. Operator access stays on the existing query protocol: last-N/since-tick queries (0x410/0x411) and the extended summary (0x412/0x413). Deeper trace capture remains open, as do networking diagnostics history depth (network selftest begin/pass/fail now emit tagged structured records into the shared log stream and the status service classifies them into a net-selftest timeline kind via a bounded retained-log sweep with additive summary counters in words 16–17; per-suite detail retention and operator-facing drilldowns remain open) and runtimes |
| &#x2705; | Observability / developer | Add timeline drilldown views to the status service behind the existing timeline query tag: a `QUERY_MODE_SERVICE` mode filters the retained ring by service id and/or event kind (sentinel `0xFFFF_FFFF` = unconstrained, constraints AND-composed), skipping past already-delivered matches so clients page through bounded 5-entry replies (the drilldown reply packs total window matches into the count word's high half), and a `QUERY_MODE_SERVICE_STATS` mode answers on a new stats reply tag (0x415) with a per-service stat line — total events, first/last seen ticks within the window, and per-kind counts (up to six rows packed into the 16-word envelope); host unit tests cover filter composition (service-only/kind-only/both/unconstrained plus matching count agreement), pagination bounds (page-size truncation, forward skip, skip-past-end, zero-capacity buffers, ring eviction window bounds), and stat-line reply packing; operator access stays on the unchanged query protocol tags (0x410/0x411) |

| &#x1F7E8; | Developer workflows / infra | Partial next step: a loopback LIVE accept-path harness landed in developer-service — compile-time gated behind `SERVICEOS_FARM_SELFTEST=1` at userspace build time (`option_env!`, so default boots are unchanged), it queues one synthetic remote-target job through the standard slot/route model (RemoteFarm route + PendingRemote export against a `self@127.0.0.1:<port>` endpoint), arms a real TCP listener through the network service's SocketListen contract (the runtime crate exposes outbound sockets but no listen wrapper yet, so developer-service speaks that leg in raw ABI messages), connects back to itself over guest-internal loopback, sends a minimal `FARMQ1 <job-id> <target>` dispatch frame, runs the in-process accept half of the wire protocol (magic+id validation, exact-id ACK framing), flips the queued job Queued→Running→Succeeded on acknowledgement, and emits greppable boot evidence (`farm-selftest PASS job=... target=linux-x64 endpoint=127.0.0.1:<port>` plus an end ok=N summary line; FAIL logs name the exact stalled stage within a hard bounded spin budget so boots can never hang). A local control tag pair (0xd22 request / 0xd23 reply) lets tooling trigger the same harness on demand without rebuilding. Wire framing, id validation, ack matching, and the queue-state transitions are unit-tested on host; the activation path additionally required declaring `depends=network-service` / `grant=network-service:send` on the developer-service bundle manifest (previously even the registry probe could never see the transport); payload upload, remote build execution, and farm authentication remain open |

## 18. Toward a More Complete OS

| Status | Area | Work Item |
|---|---|---|
| &#x2B1C; | Accounts / identity | Partial: service-side foundation landed in `account-service` — account store (id/name/display-name, per-account salted credential hashes using an honestly non-cryptographic FNV-based KDF) persisted via storage-service contracts; login/logout claim session ownership over session-service session ids and identity switching re-binds the active claim across sessions mirroring handoff semantics; per-account default capability grant sets recorded and exposed for future enforcement points; host unit tests cover hashing, the login state machine, switch semantics, policy defaults, and store serialization. Activation is manual (image built into the boot store, spawned by path via stored-image launch; not in the default boot graph and not registered under a named `ServiceId` pending a shared-ABI slot). Operator surfaces landed in `shell-service`: `whoami` now renders the full session binding (account/id/capabilities plus binding=kind/session-id), and a new `su <name> <secret>` switches an operator session's identity by dropping the previous claim through the LOGOUT contract and re-authenticating through the LOGIN contract, driven by a pure host-tested verdict mapping that covers unowned→bound, owned→switched, and both failure paths (unavailable/rejected leaves the session honestly unowned); account-service's credential-free `switch_user` claim-handoff primitive stays service-facing until a multi-session graphical surface exists. Shell-driven stored-image launches now pass an announcer channel with relay-capable rights so manually-activated services can publish their public channel back to the shell per the kernel capability-transfer rule. Graphical login UI, a real password KDF, and enforcement across storage, apps, runtimes, and services remain open |
| &#x2B1C; | Backup / restore | Partial: operator-level foundation landed in `backup-service` — scoped export snapshots the config-service namespace trees, the account store (`state/account/accounts.cfg`), and installed-package manifests into a single versioned backup blob ("SBV1" magic + format version + FNV-1a integrity checksum as an honestly non-cryptographic placeholder) persisted via storage-service contracts under `backups/<timestamp>`; restore re-validates magic/version/checksum before any write and supports scoped filtering plus a dry-run report mode; snapshot listing and deletion are path-confined to `backups/`. Host unit tests cover blob header roundtrip, checksum/header validation, scope-filter selection, and snapshot-name formatting. Activation is manual (image built into the boot store as `services/backup-service/program.img`, spawned on demand via stored-image launch; not in the default boot graph and not registered under a named `ServiceId`, mirroring `account-service`). Cross-machine migration tooling, a desktop backup/restore UI, incremental or deduplicated snapshots, and cryptographic signing of blobs remain open |
| &#x2B1C; | Printing / peripherals | Partial: foundation landed in a new `peripheral-service` (manual activation, mirroring account-service/backup-service/power-service: image in the boot store as `services/peripheral-service/program.img`, spawned on demand via stored-image launch, no named `ServiceId`) — a fixed-capacity device registry whose ATTACH contract classifies client-reported descriptors into known classes built from the kernel's enumeration vocabulary (input subclasses from the shared ABI's input_device_class set — keyboard/pointer/tablet — plus block/display/audio/printer families), monotonic never-reused device ids, a bounded attach/detach event ring with per-event sequence/tick/class detail and lifetime totals, LIST with class filtering over packed record words, a STATUS summary reply, and a printer class stub that returns the full query shape (status/queue-depth/queue-capacity) with an honest `Unimplemented` status since no print pipeline exists; the shell gained desktop-facing operator surfaces (`peripheral status|list [class]|events [n]|printer`) that launch the service on demand through the same stored-image path as account commands and degrade to notices when it is absent. Host unit tests cover class mapping incl. unknown-family rejection, registry capacity/monotonic-id/filter behavior, event-ring ordering and retention math, packed-word roundtrips, protocol-level attach/list/events/detach/status/printer flows, and error codes. Manual-activation services receive no kernel device transports at spawn, so the v0 registry fills only via client-reported descriptors rather than live enumeration — wiring real input/block/display handle injection, printer transports, hotplug detection, and richer desktop management remain open. Boot verification: headless QEMU boots stay desktop-ready and shell-ready with zero faults and the shell's on-demand stored-image launch of the new service is serial-proven (`peripheral-service ready; registry empty`); the shell↔service request path over the new announcer handshake is fixed per the kernel capability-transfer rule (send-only masks are not forwardable through the manager) but its end-to-end QEMU confirmation hit the boot budget |
| &#x2B1C; | Peripherals / wireless | Add broader peripheral connectivity including Bluetooth input devices, wireless accessories, and consumer-device management flows |
| &#x2B1C; | Power / devices | Partial: pragmatic honest foundation landed in `power-service` — SLEEP_INHIBIT/ALLOW policy flags over a refcounted inhibit registry (cookie-based acquire/release, capacity-bounded), a prepare-for-suspend broadcast contract stub that fans events out to registered listener channels with monotonic sequence numbers (dry-run only: actual S3 entry is not implemented and is not reliably exercisable under QEMU TCG), battery/power-source reporting built on an AML-less ACPI walk helper set (RSDP checksum-validated discovery, XSDT/RSDT entry parsing, compressed-EISAID scanning for PNP0C0A) plus a secondary PM-port status-sample probe, both shipping as pure host-tested logic that reports graceful absence states at runtime because userspace has no port-IO or physical-memory transport yet (kernel ACPI tables remain kernel-side read-only reference); thermal sensors are reported unavailable in v0, and the periodic device-health snapshot honestly covers monotonic uptime ticks plus inter-sample delta with drift-vs-wall-clock and memory-pressure marked unavailable until PIT/RTC and MemoryStats contracts are exposed through the ABI. Host unit tests cover EISAID encoding against the known PNP0501 vector, RSDP/root-table parsing, battery probe state coverage, the inhibit refcount state machine, listener capacity/roundtrip, broadcast planning, health sampling math, and status formatting. Activation is manual (image built into the boot store as `services/power-service/program.img`, spawned on demand via stored-image launch; not in the default boot graph and not registered under a named `ServiceId`, mirroring account-service/backup-service). Real ACPI control-method battery data, S3 suspend/resume, thermal thresholds, and laptop-oriented desktop behavior remain open |
| &#x2B1C; | Installation / onboarding | Partial: serial-first first-boot onboarding plus recovery-environment groundwork landed — a new `setup-wizard` graph service (catalog-registered, depends on storage+config) runs a serial-driven setup state machine on first boot (hostname persisted through config-service as the packed `system.hostname` key, timezone free-text under `state/setup-wizard/`, admin account created through the account-service CREATE contract via a new launch-handshake so dynamically launched services can publish their public channel), writes `state/setup-wizard/firstboot.done`, and exits cleanly; later boots skip via the done-marker before any interaction, clean exits no longer log as failures, and host unit tests cover step sequencing, defaults-on-timeout, retry semantics, and hostname packing. Recovery groundwork: the validated `SERVICEOS_BOOT_MODE=full|reduced|safe|recovery` selector now reaches all four graph-capable loaders, `cargo xtask recover` remains the recovery shortcut, and `BootMode::Recovery` still brings up only storage+console+backup-service (backup-service is now a graph-visible on-demand service that recovery activates), pruning everything else. Verified headless in QEMU: fresh-image boots drive all wizard steps over serial with documented defaults; hostname/timezone persistence and wizard exit paths confirmed. Open: graphical installer/updater surfaces, admin-account creation needs one more boot to confirm end-to-end after the launch-handshake change (final verification hit the boot budget; the wizard fails safe by retrying setup until the marker lands), recovery-mode restore UX beyond the minimal graph, and locale/user-agreement steps |
| &#x2B1C; | Hardening / release | Partial: release-engineering tooling landed in `xtask`. `cargo xtask release` builds all four registered platforms sequentially and writes `target/release/RELEASE-MANIFEST.json` recording every staged artifact (path, size, FNV-1a64 hash — sha256 deferred until a hashing dependency is added) plus explicit reproducibility notes (FAT image timestamps from mformat/mcopy, host filesystem timestamps on staged files, unpinned toolchain/host tools); honest per-platform statuses cover current release-profile gaps: `release`, `release+debug-userspace` (nested userspace links currently fail under rust-lld `.text`/`.got` overlap with the PIC + large code-model flags, so bootstore.bin falls back to debug-built programs), `debug-fallback` (full debug image when release cannot build), or `failed` with reason — today raspi5/virt fail because arch/aarch64 traps.rs does not compile for the target (missing `deadline_ticks` pattern field, E0027, both profiles) and qemu-isa inherits the x86 userspace failure through its embedded catalog. `cargo xtask test-upgrade` automates the boot-upgrade-boot matrix on qemu-virtio headless: bounded-timeout serial capture with marker-driven early exit, factory-fresh zeroed data volume, storage selftest evidence on v-current, rebuild, reboot verifying the `selftest mount-present restored=1` persistence marker. `cargo xtask validate` bundles workspace check + workspace tests + a bounded headless qemu-virtio boot grepping storage/net/audio selftest markers plus an aarch64 virt boot when buildable (currently reported SKIPPED due to the pre-existing arch-aarch64 E0027) into one summary table with a single exit code; bounded boots use throwaway OVMF vars copies so killed runs cannot poison interactive firmware state. Artifact signing and installer images remain open |
