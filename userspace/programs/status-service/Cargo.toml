[package]
name = "serviceos-status-service"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "serviceos-status-service"
path = "src/main.rs"

[dependencies]
serviceos-abi = { path = "../../../shared/abi" }
serviceos-userspace-runtime = { path = "../runtime" }
