[package]
name = "serviceos-monitor-app"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "serviceos-monitor-app"
path = "src/main.rs"

[dependencies]
serviceos-userspace-runtime = { path = "../runtime" }
serviceos-desktop-ui = { path = "../ui" }
