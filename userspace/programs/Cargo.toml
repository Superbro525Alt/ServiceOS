[workspace]
resolver = "3"
members = [
    "runtime",
    "root-manager",
    "storage-service",
    "log-service",
    "config-service",
    "console-service",
    "status-service",
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
