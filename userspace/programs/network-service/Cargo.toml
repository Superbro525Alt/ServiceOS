[package]
name = "serviceos-network-service"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "serviceos-network-service"
path = "src/main.rs"

[dependencies]
serviceos-crypto = { path = "../../../shared/crypto" }
serviceos-userspace-runtime = { path = "../runtime" }
serviceos-ssh = { path = "../../../shared/ssh" }
serviceos-wireless = { path = "../../../shared/wireless" }
smoltcp = { version = "0.12", default-features = false, features = ["medium-ethernet", "proto-ipv4", "proto-ipv6", "iface-max-addr-count-4", "socket-icmp", "socket-dhcpv4", "socket-tcp", "socket-udp"] }
